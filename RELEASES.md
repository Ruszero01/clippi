# Version 0.1.3

### 新功能

- **文本修整工具** — 编辑面板新增文本修整按钮，去除多余空格/空行/Unicode空白字符，智能合并段落
- **Base64 解码工具** — 编辑面板新增 Base64 解码按钮，支持标准/URL安全/DataURL 三种格式
- **二维码识别** - 新增二维码识别功能，支持自动识别开关和手动识别入口

### Bug 修复

- 被动定时拉取失效 — `LocalFolderBackend::pull()` 中 `mtime_changed` 在远程文件 mtime 变化时未正确设为 `true`，导致定时同步永远返回「已是最新」跳过拉取；手动同步因 `force_push=true` 绕过该逻辑不受影响
- 取消收藏同步异常 — `build_snapshot` 在 `sync_favorites_only=false` 时将取消收藏条目同时写入 `items[]`（`is_favorite=false`）和 `unfavorited_items[]`（墓碑），造成云端双列表冗余；修复为：存在墓碑的 `is_favorite=false` 条目从 `items[]` 排除，由墓碑作为取消收藏信号的唯一来源

### 优化

- 主动拉取 — 同步主开关开启、各后端独立开关开启、软件启动时主动触发一次拉取，有变更就合并推送，无变更则跳过
- 敏感信息预览脱敏 — 邮箱仅显示 local-part 前两位和域名（`jo***@gmail.com`），手机号仅显示前三位和后四位（`138****5678`），复制/粘贴/搜索仍使用完整内容
- 缓存自动清理 — 启动时后台清理数据库已无引用的孤立图片/缩略图，以及超过 30 天未访问的图标缓存，防止缓存目录无限膨胀

# Version 0.1.2

### 新功能

- **WebDAV 同步后端** — 支持 NAS、Nextcloud 等 WebDAV 服务器作为同步后端，Basic Auth 认证，ETag 增量拉取
- **同步后端独立管理** — 每个后端支持独立开关和独立同步间隔，配合 30s 连接状态缓存避免高频 HTTP 请求
- **JSON 格式化工具** — 编辑面板新增 JSON 格式化按钮，一键美化压缩 JSON 为缩进格式

### Bug 修复

- macOS 双击粘贴失效 — CGEvent Cmd+V 修饰键标志位颠倒，系统将 Cmd keyDown 误当 key repeat 处理
- 触控板滚动列表跳跃 — 卡片自动高度从 7 个档位（68/80/96/104/108/130/200px）缩减为 3 档（68/96/128px），消除相邻卡片微小高度差导致的滚动抖动
- 被动拉取时同步统计不准确 — 语义哈希守卫在快照大小计算之前提前返回 (0, 0)，导致未参与条目创建/编辑的设备拉取后 UI 显示 0 items
- Windows 端部分文字渲染为方框 — Slint 默认字体不含 CJK 字形，补注册系统微软雅黑字体
- macOS 右键菜单偏移 — 菜单位置计算缺少主面板 36px 偏移修正，导致菜单定位偏右
- 编辑面板类型下拉缺少邮箱/电话 — 自动检测支持邮箱和电话类型，但编辑面板类型选择器仅有4种类型，现补全为6种（文本/URL/路径/颜色/邮箱/电话），保存时正确写入 meta_type

### 优化

- 卡片视觉效果优化 — 图标底板和标签底色改用中性灰，自定义标签药丸样式与时间/尺寸标签对齐
- Windows 端 OCR 效果优化 — 去除灰度转换以保留小图细节，识别结果后处理清除 CJK 字间多余空格，提升中文图片搜索命中率
- 编辑面板工具栏悬浮提示 — URL 解码和 JSON 格式化按钮增加悬浮提示标签

# Version 0.1.1

### 新功能

- **侧边标签栏** — 窗口左侧新增标签筛选栏，支持展开/折叠动画、右键固定常用标签
- **版本更新检查** — 启动时自动检查 GitHub Release，托盘菜单可手动检查并跳转下载页
- **邮箱/电话识别** — 剪贴板检测到邮箱或电话号码时卡片显示对应图标
- **图片 OCR** — Windows (WinRT) / macOS (Vision) 零依赖 OCR，可搜索图片内文字、右键粘贴 OCR 文本
- **URL 解码工具** — 编辑面板新增 URL 解码按钮，一键还原百分号编码

### Bug 修复

- 快捷键录制后重启丢失
- 编辑备注时关闭窗口状态残留
- 文件路径条目超长溢出
- 内部复制/OCR 粘贴产生重复条目
- 标签编辑后无法同步到其他设备（出现重复标签）
- 删除后重建同名标签被 tombstone 永久阻止同步
- 同步取消收藏/标签变更后 UI 不刷新
- 语义哈希顺序不确定导致两端互相无限覆盖
- 被删除后重建的条目/标签无法同步
- 纯本地变更被错误阻止推送
- 同步时间戳使用字符串比较，小秒秒精度不一致导致合并判断错误
- 云端合并时 tombstone 去重保留旧时间戳，导致删除事件传播异常

# Version 0.1.0

首个公开版本。基础剪贴板管理、标签系统、云同步（OneDrive/iCloud 文件夹）、多格式支持（文本/图片/文件/链接/颜色/富文本）。
