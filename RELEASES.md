# Version 0.2.2

### Bug 修复

- **回车键粘贴与浮层/编辑冲突** — 备注内联编辑、标签选择器、确认对话框等浮层打开时，按下回车键会同时触发确认操作和粘贴；现在仅当无任何浮层或内联编辑活跃时回车才触发粘贴，浮层打开时回车只关闭浮层
- **粘贴为纯文本导致富文本格式丢失** — 富文本条目粘贴为纯文本后，剪贴板监听器将写入的纯文本重新检入并 upsert 到数据库，覆盖原有 HTML/RTF 内容导致卡片预览降级为纯文本。修复方式：upsert 前自动从同 hash 的旧数据库记录继承 `rich_data`，阻止格式数据被覆盖
- **双击粘贴忽略富文本格式** — 双击条目粘贴时读取了「捕获时转为纯文本」全局设置，导致 HTML 条目在记事本外也粘贴为纯文本。双击现在始终以完整格式粘贴，不再受该设置影响

### 优化

- **统一内容分类逻辑** — 卡片预览、类型标签、编辑面板原本各自独立判断内容展示方式，逻辑分散且不一致。新增 `DisplayKind` 枚举 + `ClipboardItem::display_kind()` 作为单一分类入口，三处统一调用
- **纯文本条目隐藏「粘贴为纯文本」** — 只有真正包含 HTML/Markdown/RTF 富文本内容的条目才显示粘贴为纯文本按钮和右键菜单选项，基于 `display_kind()` 精确判断
- **「滚动到顶部」→「定位最新条目」** — 设置项重命名为定位最新条目，开启时窗口打开自动选中并滚动到最新更改的条目；关闭时保持上次浏览位置。两种排序模式（更新时间/创建时间）均适用

# Version 0.2.1

### Bug 修复

- **构建版窗口图标不显示** — 标题栏图标改为编译期嵌入，消除工作目录依赖；Windows 任务栏和 Alt+Tab 图标改用 ICO 二进制直接解析 + `CreateIconFromResourceEx` 创建
- **固定窗口不置顶** — 窗口固定（Pin）后仍被其他窗口盖住；固定时调用 `SetWindowPos(HWND_TOPMOST)` 实现真正置顶，取消固定时恢复
- **托盘"设置"无法打开窗口** — 窗口隐藏时点击托盘菜单"设置"只切换视图不显示窗口；现在先调用 `show_and_focus` 显示窗口
- **托盘退出无效** — 退出和重启此前只清理 GPUI 状态，没有终止平台事件循环；统一改用 GPUI 标准 `quit()` 流程
- **macOS 全局快捷键失效** — GPUI 迁移时快捷键初始化被误放在 Windows 条件分支；恢复所有桌面平台的延迟注册，并合并跨平台快捷键解析逻辑
- **macOS 快速粘贴闪退** — 后台监听和快速粘贴并发访问 `NSPasteboard` 可能触发崩溃；统一串行化进程内剪贴板读写
- **macOS 构建版快速粘贴权限失效** — 启动时主动请求辅助功能权限，并为 ad-hoc 签名设置稳定 Bundle 标识，避免每次构建都被系统视为新应用
- **macOS 应用签名运行后失效** — `.app` 包内目录曾被误判为便携目录，导致数据库、配置和图片写入签名资源；安装版现固定使用 Application Support，并补充旧数据迁移
- **重复复制时间不更新** — 重复复制相同内容时 `updated_at` 不刷新导致条目无法定位到顶部；移除监听器层 hash 去重，序列号 fast-path 仍保障去重有效性
- **标签操作后列表不跟随** — 添加/移除标签后条目排序到新位置但列表没有滚动跟随；标签操作后自动找到条目新索引并滚动定位

### 优化

- **平台代码边界整理** — 快捷键生命周期、解析和录制逻辑改为桌面平台共享，仅保留物理按键检测等真实系统差异
- **macOS 发布包校验** — 发布流程自动同步 Cargo 版本到 Info.plist，并校验 Bundle ID、版本号、指定要求和代码签名
- **链接类型图标显示** — 迁移后链接类型卡片丢失网站 favicon 图标；恢复 favicon 缓存检查，渲染路径纯缓存查询不阻塞 UI，网络预取在剪贴板检测线程异步完成
- **卡片尺寸标签** — 文本/图片/文件类型卡片底部恢复尺寸标签（字符数、图片尺寸、文件大小），作为固定标签排列在自定义标签右边
- **窗口打开默认选中** — 窗口打开时自动选中最新复制的条目并定位，替代之前需按键才选中的行为；扫描 `updated_at` 最大的条目精准定位，无论排序模式

# Version 0.2.0

### 重大变更

**UI 框架全量迁移：Slint → GPUI**

  前端渲染从 Slint 迁移至 [GPUI](https://www.gpui.rs/) 0.2.2 — Zed 编辑器同款 GPU 加速 UI 框架，更适合剪贴板等文本类型应用。

**Slint 的限制：**

- ❌ **无完善富文本渲染支持** — Slint 的富文本渲染支持有限，仅支持静态 markdown 渲染，无法渲染运行时富文本内容，静态渲染对于剪贴板应用来说几乎不可用，所有富文本渲染相关功能都会受限
- ❌ **列表滚动不可控** — `ListView` 没有 `scroll_to_item` API，自动高度卡片无法计算滚动偏移，键盘方向键导航无法滚动跟随，也无法实现动态高度卡片的精准定位，键盘操作对于自定义高度列表来说相当困难

**GPUI 的优势：**

- ✅ **GPU 硬件加速** — 基于 Zed 编辑器同款框架，使用平台原生 GPU 后端（DirectX/Metal/Vulkan）渲染，性能更佳
- ✅ **完整富文本能力** — 作为代码编辑器的 GUI 框架，原生富文本能力支持完善，可直接渲染 HTML 格式、Markdown 文本、颜色标签色块，不依赖 web 能力
- ✅ **精细列表控制** — 虚拟列表支持 `scroll_to` API，无论卡片高度是否统一，都能精确滚动到目标条目，键盘导航、条目定位跳转可自然实现
- ✅ **纯 Rust API** — 所有 UI 组件用原生 Rust 编写，享受类型安全、泛型复用；不再需要 `.slint` 标记语言中转

**迁移带来的改变：**

- **富文本预览和编辑** - 支持 HTML、markdown 类型预览和编辑
- **键盘列表滚动** - 支持键盘方向键对列表进行滚动操作
- **即时语言切换** - 切换语言无需重启应用
- **更广泛的扩展能力** - 为未来功能扩展提供基础设施

**迁移范围：**

- 所有 UI 组件（主窗口、剪贴板列表、编辑面板、设置面板、上下文菜单、标签面板等）全部用 GPUI 原生 Rust API 重写
- 窗口管理改用 GPUI Window，支持原生窗口阴影、圆角和层级控制
- 主题系统改用 GPUI 的 `ThemeAppearance` 自动跟随系统深色模式
- 列表滚动迁移至 GPUI 虚拟列表，滚动体验大幅改善
- i18n 方案完整迁移
- 移除全部 `.slint` 文件和 Slint 构建依赖（`slint`、`slint-build` crate）

### 新功能

- **粘贴为纯文本** - 已支持编辑和预览富文本功能，配套支持粘贴为纯文本选项
- **Enter 键粘贴** - 已支持键盘滚动列表，补齐键盘快捷操作逻辑
- **便携模式** - 数据支持默认保存至安装目录，若安装目录不可写则回退到系统默认路径，版本升级支持自动迁移，也可在 设置 - 数据 中重置为便携模式/非便携模式

### Bug 修复

- 窗口在焦点切换期间闪现关闭
- MacOS 高版本快速粘贴权限问题导致快速粘贴失效

### 优化

- 二维码类型显示完整图片，与图片类型区分，提升视觉辨识度

# Version 0.1.3

### 新功能

- **文本修整工具** — 编辑面板新增文本修整按钮，去除多余空格/空行/Unicode空白字符，智能合并段落
- **Base64 解码工具** — 编辑面板新增 Base64 解码按钮，支持标准/URL安全/DataURL 三种格式
- **二维码识别** - 新增二维码识别功能，支持自动识别开关和手动识别入口

### Bug 修复

- 被动定时拉取失效 — `LocalFolderBackend::pull()` 中 `mtime_changed` 在远程文件 mtime 变化时未正确设为 `true`，导致定时同步永远返回「已是最新」跳过拉取；手动同步因 `force_push=true` 绕过该逻辑不受影响
- 取消收藏同步异常 — `build_snapshot` 在 `sync_favorites_only=false` 时将取消收藏条目同时写入 `items[]`（`is_favorite=false`）和 `unfavorited_items[]`（墓碑），造成云端双列表冗余；修复为：存在墓碑的 `is_favorite=false` 条目从 `items[]` 排除，由墓碑作为取消收藏信号的唯一来源

### 优化

- 主动拉取 — 同步主开关开启、各后端独立开关开启、软件启动时主动触发一次拉取，有变更就合并推送，无变更则跳过
- 敏感信息预览脱敏 — 邮箱仅显示 local-part 前两位和域名（`jo***@gmail.com`），手机号仅显示前三位和后四位（`138****5678`），复制/粘贴/搜索仍使用完整内容
- 缓存自动清理 — 启动时后台清理数据库已无引用的孤立图片/缩略图，以及超过 30 天未访问的图标缓存，防止缓存目录无限膨胀

# Version 0.1.2

### 新功能

- **WebDAV 同步后端** — 支持 NAS、Nextcloud 等 WebDAV 服务器作为同步后端，Basic Auth 认证，ETag 增量拉取
- **同步后端独立管理** — 每个后端支持独立开关和独立同步间隔，配合 30s 连接状态缓存避免高频 HTTP 请求
- **JSON 格式化工具** — 编辑面板新增 JSON 格式化按钮，一键美化压缩 JSON 为缩进格式

### Bug 修复

- macOS 双击粘贴失效 — CGEvent Cmd+V 修饰键标志位颠倒，系统将 Cmd keyDown 误当 key repeat 处理
- 触控板滚动列表跳跃 — 卡片自动高度从 7 个档位（68/80/96/104/108/130/200px）缩减为 3 档（68/96/128px），消除相邻卡片微小高度差导致的滚动抖动
- 被动拉取时同步统计不准确 — 语义哈希守卫在快照大小计算之前提前返回 (0, 0)，导致未参与条目创建/编辑的设备拉取后 UI 显示 0 items
- Windows 端部分文字渲染为方框 — Slint 默认字体不含 CJK 字形，补注册系统微软雅黑字体
- macOS 右键菜单偏移 — 菜单位置计算缺少主面板 36px 偏移修正，导致菜单定位偏右
- 编辑面板类型下拉缺少邮箱/电话 — 自动检测支持邮箱和电话类型，但编辑面板类型选择器仅有4种类型，现补全为6种（文本/URL/路径/颜色/邮箱/电话），保存时正确写入 meta_type

### 优化

- 卡片视觉效果优化 — 图标底板和标签底色改用中性灰，自定义标签药丸样式与时间/尺寸标签对齐
- Windows 端 OCR 效果优化 — 去除灰度转换以保留小图细节，识别结果后处理清除 CJK 字间多余空格，提升中文图片搜索命中率
- 编辑面板工具栏悬浮提示 — URL 解码和 JSON 格式化按钮增加悬浮提示标签

# Version 0.1.1

### 新功能

- **侧边标签栏** — 窗口左侧新增标签筛选栏，支持展开/折叠动画、右键固定常用标签
- **版本更新检查** — 启动时自动检查 GitHub Release，托盘菜单可手动检查并跳转下载页
- **邮箱/电话识别** — 剪贴板检测到邮箱或电话号码时卡片显示对应图标
- **图片 OCR** — Windows (WinRT) / macOS (Vision) 零依赖 OCR，可搜索图片内文字、右键粘贴 OCR 文本
- **URL 解码工具** — 编辑面板新增 URL 解码按钮，一键还原百分号编码

### Bug 修复

- 快捷键录制后重启丢失
- 编辑备注时关闭窗口状态残留
- 文件路径条目超长溢出
- 内部复制/OCR 粘贴产生重复条目
- 标签编辑后无法同步到其他设备（出现重复标签）
- 删除后重建同名标签被 tombstone 永久阻止同步
- 同步取消收藏/标签变更后 UI 不刷新
- 语义哈希顺序不确定导致两端互相无限覆盖
- 被删除后重建的条目/标签无法同步
- 纯本地变更被错误阻止推送
- 同步时间戳使用字符串比较，小秒秒精度不一致导致合并判断错误
- 云端合并时 tombstone 去重保留旧时间戳，导致删除事件传播异常

# Version 0.1.0

首个公开版本。基础剪贴板管理、标签系统、云同步（OneDrive/iCloud 文件夹）、多格式支持（文本/图片/文件/链接/颜色/富文本）。
