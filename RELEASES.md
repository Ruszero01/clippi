# Version 0.3.0

### 优化

- **界面 UI 优化** — 部分场景界面优化
- **动效优化** — 部分场景新增轻量过渡动画，优化视觉效果
- **标签面板输入框优化** — 标签面板输入框功能扩展，搜索/创建标签二合一，回车可智能创建新标签或激活已有标签，支持拼音搜索
- **悬浮工具栏标签操作** — 卡片悬浮工具栏新增标签按钮，位于备注和收藏之间，点击可快速为当前条目或批量选择的条目添加/移除标签
- **自动清理旧安装包残留** — 下载更新前自动清理临时目录中过期安装包
- **自动解析URL** — 复制链接时自动获取页面标题，卡片展示站点名和标题，URL 无协议前缀也能识别；支持设置开关，默认开启
- **路径类型增强** — 路径卡片展示末级目录和完整路径，底部显示盘符标签（含卷标名），支持搜索盘符筛选；UNC 路径和 IP 地址路径可识别跳转
- **源丢失检测** — 路径、图片、单文件类型自动检测源文件是否存在，失效时红色文字和标签标注，跳转按钮自动隐藏；多文件暂不检测
- **跨平台路径** — 非当前平台格式的路径不显示跳转按钮，黄色标签标注来源平台，支持设置开关过滤隐藏
- **悬浮工具栏优化** — 不再阻止列表滚动；右键菜单滚动时自动关闭

### Bug 修复

- **快速窗口呼出不稳定** — 简化快速窗口关闭逻辑，修复快速窗口触发不稳定的问题
- **标签面板无法滚动** — 标签过多时面板高度固定不变，现已支持滚动
- **部分 URL 和路径无法跳转** — 修复无协议前缀 URL 和 IP 地址路径点击打开位置无反应的问题

---

# Version 0.2.9

### 优化

- **渲染优化**
  - 卡片内容文本按需加载，不再每帧复制完整文本
  - 快速窗口标签列表仅加载已固定的标签，减少无用数据拷贝
  - 应用图标和文件图标缓存目录在启动时预创建，渲染过程零磁盘写入
  - 图标缓存未命中时不再阻塞界面，改为后台线程处理
- **同步可靠性增强** — 同步过程中数据库操作异常（如记录删除标记、更新收藏状态等）添加详细日志记录，便于排查同步问题
- **数据库检查点异常日志** — 窗口关闭和剪贴板变更时触发的数据库持久化操作，异常情况写入日志便于诊断
- **macOS 快速窗口视觉优化** — 快速窗口恢复系统阴影并保持圆角裁剪，提升浮动面板层次感
- **代码结构优化**
  - 剪贴板内容检测逻辑按类型拆分为独立子函数，维护性更好
  - 设置页开关控件提取为公共组件，减少重复代码
  - 热键录制卡片提取为公共组件，主热键和快速热键 UI 保持一致
  - 同步后端添加/编辑操作提取公共辅助函数

### Bug 修复

- **WAL 检查点静默失败** — 修复数据库变更持久化失败时无任何提示的问题
- **macOS 快速窗口光标定位** — 增强 Accessibility 光标范围解析，支持浏览器 text marker 路径，过滤窗口级错误矩形；无法获取可靠光标时回退到鼠标位置，并优化光标底部对齐

---

# Version 0.2.8

### 新功能

- **自动更新** — 支持从 GitHub Releases 自动检查、下载、校验并安装新版本
  - 启动时和每 24 小时自动静默检查更新，发现新版本时托盘图标显示圆点提示
  - 设置新增「版本」页面，显示当前版本、更新状态、Release Notes，一键下载更新
  - 下载完成后 SHA256 校验，自动重启安装
  - 下载和重启操作通过 Toast 通知展示，支持亮色/暗色主题适配

### Bug 修复

- **快速窗口定位问题** — 快速窗口光标定位沿用了记录上一个活动窗口的逻辑，但由于快速窗口不会抢走焦点，导致获取目标不准确，改用实时前台窗口定位，提升定位稳定性
- **多显示器 DPI 适配** — 修复 Windows 在不同缩放比例的显示器间呼出主窗口或快速窗口时，GPUI 缩放比例未及时刷新、窗口定位偏差、首次启动未恢复用户记录的窗口尺寸，以及快速窗口边缘出现空隙的问题

### 优化

- **优化快速窗口 UI** — 优化列表圆角，优化四边间距
- **托盘入口窗口定位优化** — 托盘菜单（显示窗口、设置、检查更新）打开窗口时始终使用记住位置模式，避免窗口贴屏幕底部边缘打开的问题
- **检查更新导航优化** — 托盘"检查更新"改为自动打开设置→版本页面，方便直接查看更新状态和下载更新

---

# Version 0.2.7

### Bug 修复

- **数据目录重置按钮无响应** — 修复非便携模式下点击"重置"按钮无反应的问题
- **便携模式反复跳变** — 修复便携模式检测不稳定，导致数据目录在 exe 目录和系统目录之间来回切换的问题
- **系统主题无法实时跟随** — 修复设置主题为"跟随系统"时，切换 OS 深色/浅色模式需重启才能生效；同时修复快速窗口在"跟随系统"模式下始终显示为暗色主题的问题 (#18)
- **快速窗口冷启动无法跟随输入光标** — 修复刚启动 Clippi 时，快速窗口无法定位到当前应用输入光标位置的问题

### 优化

- **数据目录切换智能合并** — 切换数据目录时，如果目标位置已有数据，自动合并数据库和配置文件，不再直接覆盖导致数据丢失
- **快速窗口备注优先显示** — 有备注的条目优先显示备注文字，邮箱和电话类型自动脱敏处理；开启"悬停显示原文"设置后，选中有备注的条目显示原文，与主窗口行为对齐
- **快速窗口标签栏弹性布局** — 固定标签少时按文字自然宽度显示，满一行时自动平分宽度；标签文字过长时省略号截断并显示悬停提示；无固定标签时自动隐藏标签栏并收起窗口高度

---

# Version 0.2.6

### 新功能

- **快速粘贴窗口** — 新增可独立开启（设置 - 通用 - 快速粘贴窗口）和配置快捷键（设置 - 快捷键 - 快速窗口快捷键）不抢焦点的快速窗口，在系统应用中优先跟随光标位置，仅支持类型筛选、固定标签、快速粘贴等基础操作；可使用 `↑/↓` 选择、`←/→` 翻页、`1-5` 数字键直接粘贴当前页条目、支持鼠标点击和滚轮导航、`Enter` 粘贴、`Esc` 关闭，呼出时不会抢走应用焦点，适合在不打断输入的场景下进行快速粘贴，编辑等高级操作扔需打开主窗口进行
- **编辑面板智能类型切换** — 在编辑面板中将富文本类型（HTML/Markdown）切换为纯文本类型（链接、路径等）时，自动剥离 HTML 标签并规范化空白行，仅显示纯文本；切回富文本类型时，自动恢复原始标签；在纯文本模式下编辑的内容会同步回 HTML 文本节点中
- **键盘快捷键支持** — 为剪贴板列表和搜索栏添加完整键盘快捷操作：`Shift+Enter` 粘贴为纯文本、`Ctrl+F` 聚焦搜索框、`Ctrl+D` 切换收藏、`Ctrl+E` 编辑、`F2` 添加/编辑备注、`Delete` 删除、`Escape` 清除选择；↑↓ 导航条目；搜索框聚焦时 ↑↓ 可在不失焦的情况下导航列表，按操作快捷键时自动切回列表并执行对应操作

### 优化

- **快捷键录制响应** — 录制期间使用独立快速采样，短按一次即可识别，录制完成后立即刷新设置界面

### Bug 修复

- **托盘菜单进入设置时显示 Clippi 自身** — 修复右击托盘图标弹出菜单时，焦点监听器误将 Clippi 进程内的弹出菜单窗口识别为外部应用，导致快捷键设置页的"当前应用"栏显示 Clippi 自身，可能造成误加入黑名单或误配置粘贴快捷键的问题
- **静默启动窗口闪烁** — 修复静默启动（开机自启）时窗口轮廓和任务栏图标短暂闪现的问题。通过 GPUI `WindowOptions { show: false, focus: false }` 让窗口在创建时就保持隐藏，首帧渲染后释放 in-memory 条目列表，同时保持低内存占用
- **编辑保存后类型未即时刷新** — 修复编辑面板保存 link/path/color 类型条目后，卡片上的类型标签和图标未即时更新（需重开窗口才能正确显示）的问题。根因是 `storage_for_editor_type` 将 link/path/color 存为旧版 content_type 格式，未写入 v4 meta_type，导致 `display_kind()` 无法识别，回退为纯文本显示
- **macOS 路径选择未响应** — 修复更改数据库路径或添加本地文件夹同步后端时，原生文件选择窗口可能让应用进入未响应状态的问题。文件对话框改为异步执行，避免 GPUI 主线程等待对话框结果，同时保留 Windows 端规避 COM 对话框冲突的修复 (#16)

---

# Version 0.2.5

### 新功能

- **可自定义类型筛选栏** — 右键筛选栏可配置类型按钮的显示/隐藏和排列顺序，窗口较窄时自动切换为纯图标模式，配置持久化保存
- **扩展筛选类型** — 类型筛选按钮新增图片、路径、联系信息（邮箱和手机号），各类型独立筛选不再捆绑
- **独立粘贴快捷键配置** — 快捷键设置页新增按进程配置粘贴快捷键功能，可为不同应用录制自定义粘贴组合键（如 Shift+Insert），支持列表管理、重新录制和确认对话框键盘操作（Enter 确认 / Esc 取消）
- **拼音搜索** — 支持用全拼和首字母缩写搜索中文内容

### 优化

- **统一内容类型分类** — 优化内部类型存储结构，链接、路径、颜色归为纯文本的子类型，与邮箱、手机号保持一致
- **编辑面板可拖拽分隔手柄** — 富文本编辑模式下，内容编辑区和预览区之间新增拖拽手柄，可手动调整两部分比例，初始各占一半，自适应窗口大小
- **编辑面板工具按钮提示文本** — 为编辑面板工具栏按钮（返回、URL解码、Base64解码、JSON格式化、文本修剪）添加鼠标悬停提示
- **标签面板按钮提示文本** — 为标签筛选面板的功能按钮（切换筛选模式、清空筛选）和标签选择器清空按钮添加悬停提示
- **更新图标** — 替换右键菜单粘贴图标和 OCR 图标，更新标签面板清空按钮和文件筛选按钮图标
- **统一下调 Tooltip 字体** — 全局 tooltip 提示文本字体大小从默认统一下调
- **同步状态指示灯优化** — 指示灯改用图标字体渲染，避免像素偏移
- **悬浮工具栏图标优化** — 链接、路径、文件类型的"打开位置"按钮改用线条风格独立图标（浏览器 \u{e643}、文件夹 \u{e609}），视觉风格与其他工具栏按钮统一
- **右键菜单补充打开操作** — 链接、路径、文件类型右键菜单新增"打开链接"/"打开文件夹"选项，与悬浮工具栏功能对齐
- **智能粘贴快捷键** — 粘贴时自动检测终端/控制台窗口（如 Windows Terminal、conhost），使用 Shift+Insert 替代 Ctrl+V，解决终端中粘贴失效问题

# Version 0.2.4

### 新功能

- **屏蔽系统窗口行为** — 通用设置中新增开关，可禁用双击标题栏全屏和窗口贴边缩放等系统默认窗口行为，默认关闭 (#15)
- **自动聚焦搜索框** — 剪贴板设置中新增开关，打开窗口时自动聚焦到搜索框，默认关闭

### 优化

- **卡片自定义标签溢出显示** — 空间不足时隐藏超出的自定义标签并显示 `+N` 数量，窗口宽度变化后自动更新，避免标签被裁切
- **文件类型预览优化** — 多文件预览从 3 个扩展到 4 个；每行使用各自真实的系统文件图标（优先缓存，同类型文件共用缓存），不再使用通用图标；左侧类型标签固定显示"文件"，文件数量已在标签中展示

### 稳定性

- 增强剪贴板监听和同步服务的异常恢复能力，避免个别错误导致监听中断
- 优化数据库查询性能，提升大量条目时的筛选和列表响应速度
- 改进数据序列化和同步时间戳解析的容错性，减少潜在的同步异常

# Version 0.2.3

### Bug 修复

- **语言切换后输入框文本未更新** — 切换语言后，搜索框、标签面板、编辑面板和后端配置中的提示文本会立即更新

### 优化

- **备注卡片高度** — 添加备注后减少卡片空白，取消备注后恢复自适应高度
- **macOS Intel 支持** — 提供 Apple Silicon 和 Intel 对应的独立安装包 (#13)
- **窗口首次显示** — 修复窗口首次打开时短暂闪现错误尺寸的问题
- **隐藏任务栏图标** — Windows 可选择隐藏 Clippi 的任务栏图标 (#14)

# Version 0.2.2

### Bug 修复

- **回车键操作冲突** — 打开编辑或浮层时，回车不再意外触发粘贴
- **富文本格式丢失** — 使用“粘贴为纯文本”后不再破坏原条目的富文本内容
- **双击粘贴格式异常** — 双击富文本条目时会保留完整格式
- **同步状态显示异常** — 未发生有效同步时不再错误刷新最后同步时间
- **多端同步统计不一致** — 不同设备会显示一致的同步条目和标签数量
- **备注无法同步** — 修改备注后可正常同步到其他设备
- **安装时未提示退出程序** — Windows 安装包可检测正在运行的 Clippi 并提示退出。由 0.2.1 及更早版本升级时，建议先手动退出旧版本
- **文件选择窗口闪退** — 修复浏览同步文件夹或更改数据库位置时可能闪退的问题

### 优化

- **内容类型显示一致性** — 卡片预览、类型标签和编辑面板对内容类型的显示保持一致
- **纯文本粘贴选项** — 纯文本条目不再显示无意义的“粘贴为纯文本”
- **定位最新条目** — 原“滚动到顶部”选项调整为“定位最新条目”，打开窗口时可自动选中并定位最近更新的内容
- **保留列表位置** — 编辑内容或标签后，列表不再突然跳动
- **界面细节** — 统一多处视觉和交互效果

# Version 0.2.1

### Bug 修复

- **构建版窗口图标不显示** — 修复标题栏、任务栏和 Alt+Tab 中的图标显示
- **固定窗口不置顶** — 固定窗口后可保持在其他窗口上方
- **托盘设置无法打开窗口** — 从托盘进入设置时会正确显示窗口
- **托盘退出无效** — 退出和重启操作可正常结束程序
- **macOS 全局快捷键失效** — 恢复 macOS 全局快捷键
- **macOS 快速粘贴闪退** — 修复剪贴板读写冲突导致的闪退
- **macOS 快速粘贴权限异常** — 改善构建版的辅助功能权限识别
- **macOS 应用签名失效** — 避免应用数据写入安装包并影响签名
- **重复复制时间不更新** — 再次复制相同内容时会刷新条目时间
- **标签操作后列表不跟随** — 添加或移除标签后会自动定位到对应条目

### 优化

- **macOS 发布包校验** — 改善 macOS 安装包的版本和签名校验
- **链接图标显示** — 恢复链接条目的网站图标
- **卡片尺寸标签** — 恢复文本字符数、图片尺寸和文件大小显示
- **窗口默认选中** — 打开窗口时自动选中并定位最近复制的条目

# Version 0.2.0

### 重大变更

- **界面架构全面升级** — 改善界面性能、富文本显示、列表滚动、键盘导航和后续扩展能力
- **富文本预览和编辑** — 支持 HTML 与 Markdown 内容的预览和编辑
- **键盘列表导航** — 可使用方向键浏览并定位剪贴板条目
- **即时语言切换** — 切换语言后无需重启应用

### 新功能

- **粘贴为纯文本** — 富文本条目可选择仅粘贴文本内容
- **Enter 键粘贴** — 可使用回车快速粘贴选中条目
- **便携模式** — 支持将数据保存在安装目录，并可在设置中切换数据位置

### Bug 修复

- 修复窗口在焦点切换期间闪现或关闭的问题
- 修复部分高版本 macOS 无法快速粘贴的问题

### 优化

- 二维码条目显示完整图片，与普通图片条目更容易区分

# Version 0.1.3

### 新功能

- **文本修整工具** — 可清理多余空格、空行和 Unicode 空白字符
- **Base64 解码工具** — 支持标准、URL 安全和 Data URL 格式
- **二维码识别** — 支持自动识别和手动识别图片中的二维码

### Bug 修复

- 修复定时同步无法拉取远端更新的问题
- 修复取消收藏状态在多端同步时异常的问题

### 优化

- **主动同步** — 开启同步或启动软件时会主动检查远端变化
- **敏感信息脱敏** — 邮箱和手机号预览默认隐藏部分内容
- **缓存自动清理** — 自动清理无效图片、缩略图和长期未使用的图标缓存

# Version 0.1.2

### 新功能

- **WebDAV 同步** — 支持使用 NAS、Nextcloud 等 WebDAV 服务同步数据
- **同步后端独立管理** — 每个同步后端可单独启用并设置同步间隔
- **JSON 格式化工具** — 支持格式化和压缩 JSON 内容

### Bug 修复

- 修复 macOS 双击粘贴失效的问题
- 修复触控板滚动列表时跳动的问题
- 修复多端同步统计不准确的问题
- 修复 Windows 部分中文显示为方框的问题
- 修复 macOS 右键菜单位置偏移的问题
- 修复编辑面板缺少邮箱和电话类型的问题

### 优化

- 优化卡片图标、标签样式和信息排列
- 提升 Windows 中文图片 OCR 的识别效果
- 为编辑面板工具按钮增加悬浮提示

# Version 0.1.1

### 新功能

- **侧边标签栏** — 支持标签筛选、展开收起和固定常用标签
- **版本更新检查** — 支持启动检查和托盘手动检查更新
- **邮箱和电话识别** — 为邮箱与电话号码显示对应类型
- **图片 OCR** — 支持识别、搜索和粘贴图片中的文字
- **URL 解码工具** — 可还原百分号编码的 URL 内容

### Bug 修复

- 修复快捷键录制后重启丢失的问题
- 修复编辑备注后关闭窗口的状态残留
- 修复过长文件路径溢出卡片的问题
- 修复内部复制和 OCR 粘贴产生重复条目的问题
- 修复多项标签、收藏、删除和重建状态的跨设备同步异常
- 修复部分本地变更无法推送的问题
- 修复同步时间和删除状态冲突的问题

# Version 0.1.0

首个公开版本，支持基础剪贴板管理、标签、文件夹同步，以及文本、图片、文件、链接、颜色和富文本等内容类型。
