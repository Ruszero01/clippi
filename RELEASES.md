# Version 0.4.6

### 修复

- **带备注卡片高度设置不生效** — 选择低、中、高卡片高度时，带备注的条目现在会正确使用所选高度；仅在自动模式下保持紧凑的最低高度

---

# Version 0.4.5

### 新增

- **快捷粘贴纯文本全局热键**（设置 → 快捷键）— 新增"快捷粘贴纯文本"录制卡片，可为该功能单独绑定一个全局快捷键；触发后把剪贴板当前内容以纯文本粘贴到当前应用，适合从带格式/富文本来源复制后快速粘贴纯文本。
  - 快捷键为空即禁用（不注册、不占用、触发无效）；录制流程与主/快速热键一致，录制中提供"重置"文本按钮可随时清空当前绑定，平时不显示
  - 粘贴规则：
    - 文本 / 富文本 → 按纯文本粘贴
    - 文件引用 → 粘贴完整路径，多文件每条一行
    - 仅位图 → 不操作
    - 剪贴板为空或无法识别 → 不操作
  - 黑名单语义：前台应用命中**快捷键黑名单**（与主/快速热键一致，该应用内不触发 Clippi），或 Clippi 自身在前台时不执行

### 优化

- **单击粘贴稳定性优化** (#77)— 单击粘贴模式下，普通单击改为直接粘贴光标下的条目，不再依赖"已选中"状态：窗口在静止光标下打开时，悬停选中尚未生效也不会导致首次单击只选中不粘贴；多选状态下普通单击仍只退出多选并选中点击的条目、不会误粘贴，Shift+单击纯文本、Ctrl/Cmd+单击位图等修饰单击行为不变，传输条目仍保持打开/下载

### 修复

- **快捷键粘贴偶发失灵**（#76）— 单击粘贴模式下 Shift+鼠标左键粘贴纯文本偶尔无响应；已将修饰键释放、粘贴快捷键注入与修饰键恢复合并为一次 SendInput 批量调用，避免按键事件之间被插入多余的按键事件，同时修复热键自身修饰键（如 Alt+Shift）仍被按住时注入的 Ctrl+V 被修饰键污染而遭目标应用忽略的问题
- **富文本预览表格背景色丢失** — 兼容表格单元格的 `background` 颜色简写与 `bgcolor` 属性，从 Excel/WPS 等应用复制的单元格背景色可在历史卡片、编辑面板和快速粘贴窗口的预览中正确显示
- **失效文件清理与"已失效"标记判定统一** — 历史卡片与失效清理改用同一套路径状态分类器：界面标记"已失效"的条目按相同规则被及时清理，不再出现界面判定与清理行为不一致；清理不再依赖连续缺失观测和宽限期，路径确认缺失且非收藏的条目直接删除；删除事务内不再重复探测文件系统，避免扫描与删除之间外部应用临时文件变化导致本应清理的条目无法删除
- **WPS/Excel 表格复制内容纯文本粘贴带引号** — 从 Excel/WPS 复制的表格，单元格内换行的文本在剪贴板文本字段中带传输引号，导致"复制为纯文本 / 快捷粘贴纯文本"混入引号与多余空白；现在优先从剪贴板 HTML 表格结构解析干净文本（单元格制表符分隔、行换行分隔、还原单元格内换行与数字实体），捕获与粘贴统一走该解析；历史记录中已存在的旧条目在粘贴时也会自动修复，无需重新复制
- **文件名与 Markdown 资源引用被误识别为密钥** — 复制纯文件名，或 Markdown 链接/图片时，不再被误判为密码/API 密钥而在历史中掩码显示；文件名判定收窄为常规扩展名与文件名字符，避免把任意高熵字符串当作文件名信号

---

# Version 0.4.4

### 新增

- **Ctrl/Cmd+A 一键全选** (#73) — 在历史列表中按 Ctrl+A（macOS 为 Cmd+A）即可选中当前可见的所有条目，批量操作更顺手
- **富文本预览支持嵌入样式表** — 从 Word、WPS 等应用复制带样式表的内容后，历史卡片、编辑面板和快速粘贴窗口会按原始样式显示预览（如 WPS 表格单元格的字体颜色），不再退化为纯文本
- **快速窗口定位兜底更可靠**（Windows，#75）— 光标/插入符位置查询失败时，按“光标所在显示器 → 主显示器 → 剩余显示器”的顺序确定位置；没有可用显示器时保持隐藏，不再出现在屏幕 (0,0) 处
- **可自定义快捷粘贴方式** — 在“设置 → 剪贴板”中可选择粘贴触发方式，默认“双击粘贴”保持原有习惯；切换为“单击粘贴”后，单击已选中的条目即可直接粘贴，Shift+单击粘贴为纯文本，Ctrl/Cmd+单击可将图片以位图格式粘贴；单击未选中条目仍只负责选中，多选状态下点击仅调整选择、不会误触发粘贴

### 修复

- **macOS 多屏定位偏移** — 统一按系统主屏转换坐标，修复在不同高度的显示器间切换焦点后窗口位置与插入符定位偏移；快速窗口定位失败时使用剩余屏幕工作区兜底
- **富文本选区与样式边界** — 保留原始选中片段，避免重新粘贴时带入未选中的上下文；修复换行和图片标签后的样式泄漏，以及 Windows 富文本同步到 Mac 后的粘贴范围

- **超长截图等大图无法记录** — 支持 2000×40000 等超长截图；同时约束像素总量、解码内存和缩略图尺寸，避免超大载荷导致内存激增或预览失败
- **WPS 等富文本复制后样式丢失** — 从 WPS 表格等应用复制内容再粘贴时，不再丢失字体颜色等样式
- **插拔显示器后隐藏窗口被意外弹出**（#75）— 修复了后台运行时插/拔外接显示器会把隐藏的窗口重新调出的问题（GPUI 自身缺陷）；现在窗口会在 200ms 轮询中自动恢复隐藏，减少意外弹出的影响

---
# Version 0.4.3

### 新增

- **单键快捷键与双击触发** — 快捷键现在可直接使用波浪键、功能键等安全单键；录制时再次按下同一按键可设为双击触发，等待超时或点击按钮则确认为单击，常用字母、数字和导航键仍会受到保护以免影响正常输入

### 优化

- **窗口拖动与缩放更流畅** — 拖动或缩放窗口时不再每 200 毫秒重复写入配置文件，停止操作后统一保存一次并采用原子写入避免配置损坏；隐藏或退出时确保最终位置完整保存
- **历史列表滚动与悬停更流畅** — 卡片不再在渲染时直接探测文件路径，改为后台检测并缓存结果，断开映射盘等失效路径不会阻塞滚动和悬停
- **复制内容入库更快** — 剪贴板内容写入历史时减少重复查询：同一内容只查询一次数据库，清理超量记录时只读取需要删除的条目，标题栏统计合并为一次查询
- **搜索输入更流畅** — 快速连续输入时只执行最终的搜索，不再每敲一个字符就搜索一次；拼音匹配复用编码结果，减少重复计算
- **复制/粘贴后列表响应更快、位置更稳定** — 使用条目（复制、粘贴、粘贴为纯文本、颜色转换、OCR、图片/文件路径等）后不再整页重新加载历史，只更新对应条目的位置，列表不再跳动，滚动与选择保持稳定；上万条历史时操作更跟手
  - 按更新时间排序时，使用过的条目立即移到最新位置；按创建时间排序时列表顺序保持不变
  - 批量粘贴多条时合并为一次更新，不再逐条重复刷新，粘贴过程更顺畅
  - 开启“搜索时收藏优先”后，条目只在所属收藏/普通组内移动，分组保持稳定
  - 在类型、标签、收藏和关键词筛选下使用条目，筛选结果不会发生变化
- **大图复制更快、不卡界面**（#64）— 图片历史改为后台保存：复制大图后立即出现“处理中”占位卡片，图片保存完成后自动替换为正式条目，复制过程不再等待图片编码完成

### 修复

- **macOS 图片剪贴板捕获更可靠** — 原生 TIFF 图片改为后台转换，避免监听线程同步解码大图；对应用分阶段写入剪贴板的短暂空内容进行有限重试，减少图片漏记
- **中转站文件状态与操作异常**（#65）— 本地源文件被移动或删除后会正确显示为“云端”并允许重新下载，不再错误显示“已失效”或继续打开无效位置
- **中转站重新下载产生重复条目**（#65）— 重新下载同一文件时会恢复原历史条目并更新本地路径，保留原有收藏、标签和备注，不再新增重复记录；“永久”文件也不会因之后修改保留天数而被误清理
- **Word 复制内容预览问题**（#64）— 从 Word 复制文字后，卡片不再显示字体名称、CSS 样式等无关内容；带超链接的文字不再显示空白卡片，会正常显示链接文字
- **富文本预览异常自动回退**（#64）— 复制内容包含的富文本格式无法正确预览时，卡片自动显示纯文本内容，不再出现空白卡片
- **FastStone Image Viewer 裁切大图无法写入剪贴板**（#64）— 修复 Clippi 运行期间裁切大图后剪贴板被清空、PixPin 等也无法粘贴的问题；现在 Clippi 不再在源应用写入图片时打开剪贴板打断写入

---
# Version 0.4.2

### 新增

- **搜索时收藏优先**（#61 收藏优先部分）— 设置 → 剪贴板新增“搜索时收藏优先”开关，开启后搜索结果中收藏条目整体排在普通条目之前，无需再切换“仅显示收藏”过滤
  - 收藏组与普通组内部保持当前时间排序（创建时间或更新时间，随现有设置）
  - 搜索期间收藏或取消收藏立即按新顺序重排，无需重新输入关键词
  - 输入搜索词后列表自动回到顶部并选中第一条收藏结果；搜索中收藏/取消收藏后，自动定位到该条目新的分组位置，后续粘贴/复制/删除仍作用于当前条目
  - 开关默认关闭，升级后搜索结果顺序与之前完全一致

- **标签删除二次确认** — 删除标签前先弹出确认窗口，显示标签名称并说明影响：标签将从所有剪贴板条目中移除，条目本身不会删除
  - 支持确认按钮、`Enter`、`Esc` 和点击遮罩四种交互，取消不会改变任何数据
  - 删除失败时确认窗口保持打开并给出提示，避免误以为删除成功

### 优化

- **清理已失效条目更安全** — 条目源文件或路径消失后不再立即删除，需连续两次清理周期确认且经过至少 24 小时观察期，临时改名、移动、云盘离线等短暂状态不会误删
  - 远程盘、可移动介质、跨平台路径、来源不明及等待同步下载的内容不会被自动清理（macOS 上 `/Volumes` 下的 USB 盘、移动硬盘等外置卷，以及 iCloud Drive、File Provider 云盘路径同样保持保护）
  - 同步图片下载完成后才纳入清理，下载中不会被误删
  - 删除条目后同一轮回收其缩略图缓存；清理因文件占用等临时失败时自动延后重试，不会误记为已完成
- **删除标签自动清理置顶** — 被删除的标签自动从侧边栏置顶项中移除，不再残留失效的置顶配置
- **确认弹窗外观统一** — 设置页的删除后端、配置同步等确认弹窗移除重复遮罩并统一入场动画，明暗效果与其他确认弹窗一致

### 修复

- **清理保护状态跨存储迁移不丢失** — 切换存储模式或重置并合并数据库时，条目的存在性证据与待同步图片标记完整保留，等待下载的同步图片不会因合并被当作本地图片误删
- **中文路径下升级后同步图片保护** — 中文用户名或中文自定义数据目录下从旧版本升级，历史同步图片会被正确标记为待同步，下载完成前不会被误删

---
# Version 0.4.1

### 新增

- **图片位图粘贴快捷入口** (#52) — 主窗口新增键盘和鼠标直接入口，无需打开右键菜单即可将图片粘贴为位图
  - `Ctrl+Enter`（macOS `Cmd+Enter`）单选图片时强制粘贴为位图，其他条目或多选回退到现有默认粘贴；搜索框焦点下行为与列表一致
  - `Ctrl+双击`（macOS `Cmd+双击`）在唯一选中且选择集合未变化的图片上粘贴为位图；第一次点击改变选择时不触发粘贴，保留多选结果
  - `Shift+双击` 在唯一选中且选择集合未变化的条目上粘贴纯文本，图片条目粘贴图片路径
  - 图片卡片悬浮工具栏新增“粘贴为位图”按钮，右键菜单“粘贴为位图”同步显示 `Ctrl+Enter` 快捷键
  - 修饰双击以“第一次点击是否改变选择集合”判定意图；`Ctrl/Cmd+Shift` 及其他修饰键组合（Alt、Fn 等）不建立候选、不触发粘贴

- **配置手动同步**（#55、#59）— 设置 → 同步页新增独立的“配置同步”卡片，手动将通用设置上传到本地文件夹/WebDAV 后端，或下载应用云端配置
  - 上传前明确提示将覆盖所选后端上现有的云端配置；应用前展示云端快照的上传时间、来源平台和应用版本，并要求确认后才写入
  - 只迁移白名单内的跨设备安全设置（外观与交互、列表与内容、剪贴板功能、数据保留、同步策略、中转站策略和更新策略），快捷键、窗口位置、数据路径、同步后端凭据、应用黑名单和运行时间状态等本机相关设置始终保留
  - 应用前自动备份当前配置为 `clippi.toml.before-cloud-apply.<时间戳>.bak`，保存成功后通过受控流程重启应用生效；重启失败时配置仍保留在磁盘和内存中，可手动重启
  - 配置同步与剪贴板自动同步完全隔离，使用独立的 `clippi_settings.json` 文件，不参与自动同步、脏标记和合并冲突
  - 未知或非法的云端字段值会使整份快照被拒绝，不做部分应用；残缺快照、版本过新或时间戳无效的快照同样被拒绝

- **中转站文件固定/取消固定**（#56）— 中转站文件可固定为永久保留，固定状态保存在远端清单中并在所有设备间一致可见
  - 悬浮工具栏与右键菜单新增图钉入口；固定条目显示蓝色左侧竖条与蓝色“永久”标签
  - 固定条目永远跳过自动过期清理，但仍可手动删除；取消固定时按当前保留天数重新计算完整保留期，不会因旧过期时间立即消失
  - 中转协议升级至 v3：旧客户端会拒绝写入新清单以保护固定状态；本地文件夹后端的 v2 操作日志继续兼容物化，冲突快照合并保守保留固定状态
  - 固定/取消固定期间的重复点击自动去重，操作失败通过警告提示，不保留错误的乐观状态

- **中转站剩余时间标签**（#56）— 中转站卡片不再显示“几天前”，改为按天显示生命周期信息
  - 固定条目显示蓝色“永久”；未固定条目显示“剩余 N 天”（向上取整，剩余 24 小时以内为红色“剩余 1 天”）
  - 已到期尚未清理的条目显示红色“已到期”；全局关闭自动过期时显示灰色“永久”
  - 标签边框保持原有灰色样式，不随文字颜色变化

- **中转站关键词搜索**（#56）— 支持按文件名搜索云端清单，与普通列表共用分词、多词、大小写和拼音规则
  - 只匹配远端文件名，不搜索路径、上传设备、状态等字段；搜索在内存清单中进行，每次输入不访问网络或数据库
  - 文件名关键词高亮（含拼音命中）与普通列表完全一致，筛选与高亮共用同一套区间算法
  - 中转站模式隐藏类型与标签筛选栏，只保留搜索框；退出后搜索词与筛选状态原样恢复

### 优化

- **macOS 常驻内存优化（P0/P1）** — 依据 `docs/macos-memory-optimization.md` 消除 Quick Paste 图片驻留与后台长时漂移
  - Quick Paste 图片预览改用 310px 缩略图并绑定可释放的局部图片缓存（favicon、文件图标同样），不再加载原图进入 GPUI 无界进程级资产缓存，解决使用 Quick Paste 后内存只升不降的阶梯式增长
  - 缩略图异步生成完成后主动刷新 Quick Paste 弹窗，占位文本自动替换为缩略图
  - 新增统一资源释放事件（`ReleaseUiResources`）：隐藏窗口与静默启动先同步释放列表条目与图片缓存，再延后执行 allocator pressure relief，保证已释放页真正可回收
  - macOS 焦点监视、剪贴板轮询与前台应用信息获取按轮次建立 autorelease pool，消除长时间静默下的 Objective-C 临时对象漂移
  - 前台应用信息完整去重：名称、窗口标题与图标均未变化时跳过字符串克隆与状态更新，同时确保同名进程的图标变化仍能同步

- **macOS 隐藏后内存回落** — 隐藏主窗口或 Quick Paste 后自动释放窗口渲染占用的显存资源，内存更快回落到接近冷启动水平，长时间使用不再持续积压
  - 恢复窗口时先还原尺寸再显示，不会出现闪烁或位置跳变，原有窗口位置和激活行为不变
  - 压缩状态下退出、重启或应用云端配置，窗口尺寸和位置仍保持正常

- **复制音效播放性能** — 复制音效播放改为复用缓存的音频流，避免每次复制时重建音频流和音频设备
- **“查看图片”图标与命名统一** — 悬浮工具栏和右键菜单的“查看图片”改用独立图标（`\u{e69f}`），与“粘贴为位图”区分；菜单标题统一为“查看图片”
- **选择状态不变量修复** — 多选后使用方向键导航时同步更新选中计数和选择锚点，避免 Enter 误进入单条目的批量粘贴、`Shift+单击` 从陈旧锚点建立范围选择
- **搜索匹配与高亮算法统一** — 分词、大小写、拼音与匹配区间提取为 `core::search` 单一实现，消除普通列表、中转站和高亮渲染之间的规则漂移
- **搜索框与筛选栏拆分** — 搜索输入与类型/标签筛选拆分为独立组件，中转站模式可单独隐藏筛选栏，普通列表布局与间距保持不变

### 修复

- **快速窗口紧凑排列失效条目** — 源文件已被删除的本地图片或单文件不再占用快速窗口的显示名额与数字槽位，第一页自动补齐其他有效条目，槽位重新从 1 编号
  - 数字键、方向键、Enter 与鼠标点击的粘贴对象始终与画面高亮一致；条目在窗口打开期间失效或列表刷新后，也不会粘贴已失效条目或错选其他条目
  - 所有条目均失效时正确显示空状态，导航与数字快选不会越界
- **黑名单弹窗滚动状态隔离** — 热键黑名单、粘贴快捷键和剪贴板应用黑名单三个弹窗共用滚动状态，直接切换弹窗时会继承上一个弹窗的滚动偏移；现在每个弹窗使用独立的滚动状态键，打开时从顶部开始，互不干扰
- **应用列表超长名称截断** — 应用列表弹窗中超出宽度的应用名以省略号截断显示，不再撑破行宽被裁切
- **中转站搜索不再触发数据库查询** — 中转站模式下输入关键词直接过滤内存清单，不再逐键重新加载数据库
- **从设置页进入中转站的页面跳转** — 设置页点击中转站会先切换到列表页展示，关闭后返回原设置页及原标签；标题栏不再直接切换中转站状态，由根视图统一管理导航

---
# Version 0.4.0

### 新增

- **底部状态栏** — 主窗口底部新增统一状态栏，显示当前前台应用名称、图标和窗口标题
  - 状态栏在剪贴板列表和设置页面均可见，编辑页面自动隐藏
  - 右侧提供快捷操作入口：添加到应用黑名单、添加到快捷键黑名单

- **敏感内容脱敏扩展** — 自动识别密码、API Key、访问令牌和私钥等密钥类剪贴板内容
  - 检测覆盖 GitHub Token、OpenAI Key、AWS Access Key、Stripe Key、Slack Token、Google API Key、JWT、Bearer/Basic 认证头、PEM 私钥块以及 `password=`/`api_key=` 等显式字段名
  - 主列表卡片和快速粘贴窗口统一对密钥值做 3+4 脱敏显示（如 `sk-****WXYZ`），掩码固定四位星号不反映原文长度
  - 不超过 7 个字符的密钥仅显示 `****`，私钥块统一显示 `PRIVATE KEY · ****`
  - URL 内嵌凭据（`user:password@host`）和敏感查询参数（`?token=xxx`）在链接预览中局部脱敏，条目仍保留链接类型和 favicon；含敏感参数的链接不会自动访问完整 URL 获取标题
  - 短邮箱本地部分（≤3 字符）从完整显示改为仅显示首字符加掩码，短电话（≤7 字符）从原文显示改为仅显示 `****`
  - 编辑面板可手动将条目类型改为"密钥"或纠正误识别，"信息"类型筛选同时覆盖邮箱、电话和密钥

- **应用剪贴板黑名单**（#49） — 可为指定应用设置黑名单，其剪贴板内容不会被 Clippi 记录
  - 底部状态栏"添加到应用黑名单"快捷入口，可随时切换黑名单状态
  - 支持大小写不敏感匹配，多设备设置合并时自动去重
  - 黑名单在启动宽限期和实时捕获中均生效

- **Windows Win+V 接管**（#53） — 可将系统原生剪贴板历史快捷键 `Win+V` 重定向到 Clippi
  - "设置 → 快捷键"页提供显式的"接管系统剪贴板快捷键"开关，仅在 Windows 上可见
  - 开启前有确认对话框，明确说明操作不需要管理员权限、需注销或重启后生效、会同时禁用 Win+Ctrl+V / Win+Shift+V / Win+Alt+V
  - 接管失败或尚未生效时，原备用快捷键（默认 `Alt+V`）继续工作，不会丢失入口
  - 关闭接管时先验证备用快捷键可用，注册表撤销失败会自动回滚保持一致性

- **数据清理**（#54） — 数据设置页新增两项清理能力，并重组页面布局
  - **清理已失效条目** — 新增持久化开关，开启后可在自动缓存清理或手动"立即清理"时扫描并删除源文件/路径已不存在的条目
    - 保守判定策略：网络路径（UNC）、离线卷、异平台路径、权限不足路径和收藏条目一律跳过不删除
    - 多文件条目仅在所有源路径均确定不存在时才删除，部分缺失则保留整条
  - **清空数据** — 新增独立危险操作卡片，红色按钮配合二次确认弹窗
    - 保留标签定义、中转站条目、设置和源文件；清空后自动清理孤儿缓存
    - 为所有可同步类型（文本/富文本/图片）写入删除墓碑，防止其他设备数据回流
  - **页面布局重组** — "保留条目数"和"保留时限"合并为一张"历史保留"分组卡片，中间使用分隔线
  - 所有清理与清空操作移至后台线程执行，不阻塞 UI
  - 删除操作在数据库事务中完成（条目标签关联+墓碑+条目删除），任一步失败整体回滚
  - 文件条目删除后自动清理中转站隐藏关联，保留源文件和远端对象

### 优化

- **脱敏预览架构统一** — 邮箱、电话和密钥的脱敏渲染不再各自解析 `****` 字符串，改为使用统一的 `SensitiveText` 结构化组件，避免不同界面出现规则漂移
- **设置页弹窗迁移** — 快捷键黑名单、粘贴快捷键列表和应用黑名单的管理弹窗从内联 UI 改为统一 `app_list_dialog` 浮层组件，支持入场/退场动画
- **NAS 图片与文件预览**（#52）— UNC 路径和网络映射盘中的图片、文件改用"文件名 + 完整路径"卡片，并显示 NAS 主机标签；不再自动读取远程文件状态、真实图标、图片尺寸或缩略图，也不自动执行 OCR/二维码识别，避免复制大文件时产生额外网络和资源占用
- **快速窗口标签选中样式** — 选中标签的背景与文字颜色互换，背景改为半透明主题色叠加层、文字改为标签自身颜色，配合底部指示线使选中态在任意标签颜色下都清晰可辨
- **部分图标优化** - 优化了部分图标和尺寸
- **悬浮工具栏添加 tooltip** - 为悬浮工具栏添加文字提示

### 修复

- **WebDAV 周期同步误报失败** — 修复 WebDAV 服务返回 `304 Not Modified` 时，客户端将空响应正文误当作损坏 JSON，导致后端状态标红并中止该次周期同步的问题；未变化响应现在会被正确识别为"已是最新"
- **纯文本复制导致图片变成路径**（#57）— "复制为纯文本"设置现在仅去除文本条目的 HTML/RTF 格式，不再将图片和文件转换为路径文本；显式的"粘贴为纯文本"操作仍保留图片、文件转路径能力
- **路径文本类型归类统一** — 复制的文件系统路径文本始终归入"路径"筛选，不再根据本地文件是否存在动态归入"文件"；只有系统剪贴板中的文件对象才进入"文件"筛选

---
# Version 0.3.9

### 新增

- **文件中转站**（#41） — 复用已配置的本地文件夹或 WebDAV 后端，实现跨设备文件临时中转
  - 右键文件条目可上传，自动计算 SHA-256 校验，支持流式传输避免大文件占用内存
  - 标题栏新增入口图标，点击进入中转站视图，支持按文件名搜索排序
  - 云端条目可下载到本地，完成后自动校验 SHA-256 和文件大小
  - 已有本地副本的条目可直接打开位置
  - 支持 1/3/7/30 天或永久保留，过期文件自动清理
  - 下载/上传中状态提示，防止重复入队，成功/失败切换状态图标
  - 多设备同时操作通过乐观锁和冲突合并保护，不会互相覆盖

### 优化

- **界面布局紧凑化** — 缩小标题栏（38→30px）、搜索栏、筛选栏和卡片列表间距
  - 标签和类型筛选面板位置上移与列表顶部对齐
- **同步后端卡片改进** — 布局优化并为后端操作按钮补充悬浮提示
- **同步可靠性增强** — WebDAV 同步推送增加 ETag 乐观锁保护，本地文件夹同步支持冲突文件合并，推送冲突时自动重试

### 修复

- **文件筛选覆盖路径文本条目** — 文件类型筛选运行时根据本地文件实际状态动态分类路径文本（存在文件→归入文件筛选，不存在或目录→归入路径筛选），使用异步缓存避免阻塞界面
- **空白文件数据容错** — 空 file_data 不再导致条目渲染异常

---

# Version 0.3.8

### 新增

- **保留时限** — 数据设置新增"保留条目数"和"保留时限"两项自动清理规则。保留条目数按滑动窗口实时逐出最旧的非收藏条目；保留时限按天定期清理长期未更新的条目（使用 `updated_at`，频繁复制的条目时间会持续刷新）。两项设置为 0 即永久保留，收藏条目不受清理影响
- **图片同步与压缩** — 同步设置新增图片同步开关和图片压缩开关。开启后可将图片条目的尺寸、缩略图和远端图片 blob 一并同步；压缩模式会优先保留透明 PNG，对适合有损压缩的非透明图片使用 JPEG，减少同步传输体积
- **自定义粘贴快捷键** — 快捷键设置新增最近 10 项内容的全局粘贴快捷键；剪贴板条目右键菜单新增单条目快捷键，可为常用内容绑定专属全局快捷键，并按内容类型选择纯文本、图片路径、OCR 文本、文件路径、RGB/HEX 等粘贴格式
- **快捷键筛选** — 标题栏新增快捷键条目筛选按钮，便于只查看已绑定自定义快捷键的内容

### 优化

- **设置页英文布局适配** — 缩短设置页英文标签和 tab 标题，并为设置项文案增加省略号与收缩保护，避免窄窗口下长英文把开关控件挤出窗口
- **条目时间标签扩展** — 条目相对时间标签新增月和年显示，超过 30 天显示"1月前"等月级标签，超过 365 天显示"1年前"等年级标签
- **标题栏按钮显示优化** — 收藏和快捷键筛选按钮仅在存在对应条目或筛选已启用时显示，并为筛选、固定窗口等按钮补充悬浮提示

### 修复

- **优化错误提示**（#44）— 网络、更新、同步和数据操作失败时不再在界面显示过长的原始错误详情，改为简短提示，详细原因仍会记录到日志中便于排查，断网重启时不会反复检查并弹错误
- **混合格式条目识别错误**（#43）— 修复图片与文本格式并存时条目识别错误的问题

---

# Version 0.3.7

### 新增

- **合并条目** — 多选后右键或悬浮工具栏可"合并条目"（#36），将选中内容无缝拼接为一条新记录并自动定位；选中的全部为富文本时保留 HTML 格式
- **快速窗口收藏过滤** — 快速粘贴窗口顶部栏右侧新增收藏过滤按钮（星标图标），点击筛选仅显示已收藏条目，激活状态以金色高亮，与主窗口收藏过滤联动
- **主窗口快捷键** — 新增 `Ctrl+C` 复制到剪贴板、`Ctrl+T` 弹出标签选择器、`Space` 智能打开（图片→系统查看器、链接→浏览器、路径/文件→资源管理器）；`Backspace` 与 `Delete` 均可触发删除确认
- **右键菜单快捷键提示** — 右键菜单功能条目右侧显示对应快捷键（如"粘贴 Enter"、"收藏 Ctrl+D"），macOS 自动显示 ⌘ 符号。菜单宽度自适应适配长标签

### 优化

- **悬停即选中** — 主窗口剪贴板列表中，当仅选中一条或未选中时，高亮自动跟随鼠标悬停的条目，与悬浮工具栏行为保持同步。按住 Ctrl/Shift 多选两条及以上时冻结选中状态，避免干扰批量操作。快速窗口同样支持悬停高亮跟随
- **快速窗口滚轮翻页** — 快速窗口滚轮改为按页（5 条）滚动，保留悬停位置（如悬浮第 3 条翻页后仍选中第 3 条），滚动增量累积到阈值后触发翻页，触控板不再过度灵敏
- **快速窗口标签筛选选中态** — 固定标签改为底部横条高亮，并支持居中展开动效
- **文件路径打开行为改为定位位置** — 文本路径改为在资源管理器中跳转并选中文件（本地存在的路径）
- **邮箱和电话预览优化** — 邮箱/电话卡片改为居中加粗预览，并保留脱敏显示

### 修复

- **部分来源应用图标获取不到** — 修复内存优化后 `list_item_select_columns()` 将图标数据返回为空导致新应用图标无法缓存的问题；修复异步后台线程写入缓存导致图标延迟显示的问题；修复 WebView2 应用剪贴板拥有者为代理进程而拿到错误图标的问题；修复部分应用（如 Zed）`GetClipboardOwner()` 返回 NULL 导致完全无法检测的问题；统一前台窗口回退机制和 app 名 sanitization 规则
- **ESC 返回后快捷键失效** — 修复从设置/编辑页按 ESC 返回剪贴板列表后，键盘焦点停留在根视图导致 Space/Delete 等快捷键无响应的问题；修复窗口呼出时若未开启搜索框自动聚焦，列表无法获得键盘焦点的问题；修复删除确认弹窗缺少焦点导致 Enter/Esc 无法操作的问题

---

# Version 0.3.6

### 新增

- **快速窗口高级粘贴** — 按住 **Ctrl**——macOS 同时支持 **⌘** 或 **Ctrl**—— 弹出高级粘贴菜单（图片支持位图/路径/OCR 三种模式，颜色支持 RGB/HEX 格式切换，文件支持路径粘贴），滚轮或 Ctrl+↑/↓ 循环切换默认选项，Ctrl+点击/Ctrl+Enter 执行高级粘贴；按住 **Shift** 快速纯文本粘贴（图片和文件类型粘贴对应的文件路径，不弹窗，配合点击或 Shift+Enter 直接触发）。底部新增快捷键提示栏（macOS 优先显示 ⌘），高级粘贴偏好按类型持久化
- **文件类型路径粘贴** — 文件类型支持粘贴文件路径（快速窗口 Ctrl+Enter / 右键菜单"粘贴文件路径"），多文件路径换行分隔
- **搜索框自动清空开关** — 设置新增"打开窗口时清空搜索框"选项（#37），开启后每次呼出主窗口自动清除搜索栏内容，省去手动清除的步骤

### 修复

- **macOS 多选加选失效** — 修复 macOS 主列表中 Control+点击无法加选的问题；同时支持符合 macOS 习惯的 Command+点击切换选择，普通右键菜单行为保持不变
- **ESC 关闭窗口偶尔失效** — 修复从编辑页或设置页按 ESC 返回剪贴板列表后，键盘焦点丢失导致再次按 ESC 无法关闭窗口的问题。视图切换时主动将焦点归还给根元素，确保后续 ESC 能正常捕获
- **窗口缩放不跟手** — 修复右键菜单、标签筛选等浮层面板打开时，全屏遮罩层消费了窗口边缘的鼠标事件导致拖动缩放无反应的问题。遮罩层改用 `occlude()` 阻止点击穿透，不再消费平台级事件
- **批量添加标签多选状态被重置问题** — 批量添加标签时维持多选状态，修复同时添加多个标签需要重新选中的问题
- **仅收藏同步模式下删除未收藏条目产生无效墓碑** — 仅收藏同步模式下，删除未收藏的条目不再向云端发送删除墓碑，减少无意义的同步数据传输

### 优化

- **侧边栏标签折叠** — 当固定和激活的标签数量超出窗口高度时，自动折叠标签并显示 "+N" 溢出指示器
- **标签筛选面板 pin 状态区分** — 已固定的标签左侧色点替换为图钉图标，未固定标签保留正圆色点，视觉上更直观地区分两种状态
- **搜索与高亮增强**（#39）— 关键词搜索支持空格分隔的多关键词匹配，并统一支持正文、备注、富文本 HTML、链接、路径、OCR/二维码文本、页面标题和标签等字段；中文内容支持拼音和首字母匹配。搜索结果预览会自动定位到命中窗口并使用主题强调色高亮，富文本 HTML 会在保留原样式的同时仅高亮命中片段；HTML 搜索仅匹配可见文本，避免被标签属性或样式内容误命中。链接和路径辅助文本命中时会缩短前缀并用省略号定位到关键词附近，减少窄窗口裁切
- **内存占用与峰值优化** — 主列表改为轻量字段加载，常驻列表只保留卡片预览所需的正文、备注和富文本片段，复制、粘贴、编辑时仍按需读取完整数据；列表不再常驻来源应用图标的 base64 数据，优先复用落盘图标缓存。关键词搜索改为分页扫描并限制最大候选窗口，避免搜索时一次性加载全部历史；URL favicon/页面标题抓取改为有界后台队列，防止批量复制链接时创建大量线程；同步推送前复用已拉取的远端快照，减少一次大 payload 克隆

---

# Version 0.3.5

### 优化

- **搜索栏支持备注搜索**（#33） — 关键词搜索现在也会匹配条目的备注内容，支持中文拼音（全拼和首字母缩写）
- **快速粘贴窗口单击粘贴** — 由于快速粘贴窗口点击操作单一，优化冗余点击，改为单击条目即可粘贴，更贴合使用场景；主窗口由于点击操作耦合面太广和高级功能需求保持双击粘贴不变，键盘导航（方向键 + Enter）不受影响
- **macOS 隐藏窗口内存回收优化** — 主窗口隐藏时释放剪贴板列表数据、虚拟列表尺寸缓存和 GPUI 图片缓存，并对 macOS 所有 malloc zones 执行 pressure relief，降低滚动大量内容后关闭窗口的驻留内存
- **统一窗口隐藏内存清理行为** — 将内存释放逻辑从快速窗口隐藏方法中抽离，主窗口和快速窗口隐藏时各司其职，消除主窗口隐藏时内存清理重复触发的问题；卡片图片统一接入 retain-all 图片缓存，窗口隐藏时随缓存替换一并释放

### 修复

- **重启应用失效** — 修复托盘菜单"重启"、数据库路径变更、存储模式切换等场景下应用退出后未能重新启动的问题
- **编辑窗口无法输入** — 修复 ESC 逐级返回逻辑反复抢占编辑页焦点，导致编辑窗口不显示光标、文字无法选中和无法输入的问题

---

# Version 0.3.4

### 优化

- **Windows 前台应用检测优化** — Windows 端同样缓存当前前台进程的应用名和图标，避免轮询中重复 `OpenProcess`、查询进程路径和 HICON 转 PNG；窗口标题仍保持实时更新
- **旧版超大来源图标清理** — 启动时自动清理历史数据库中超过 256KB 的 `source_app_icon`，查询列表时也会跳过过大的旧图标字段，避免旧版本保存的原始大图标继续拖高启动内存
- **图片后处理并发限制** — OCR/二维码识别改为单后台 worker 队列处理；缩略图生成最多 2 个并发任务，降低连续复制图片时的内存峰值
- **音效播放设备跟随** — 每次播放音效时创建新的 `rodio::OutputStream`，自动使用系统当前默认音频输出设备，切换扬声器/耳机后即时生效
- **音效 200ms 防抖** — 短时间内多次触发播放时仅播放第一次，避免突发剪贴板事件导致音效叠加
- **可执行文件图标独立缓存** — `.exe`、`.dll` 等可执行文件各自提取真实图标，不再复用首个缓存图标；每个程序安装包/可执行文件现在显示自己独有的图标
- **图标缓存改用引用追踪清理** — 每日/每周缓存清理从"删除 30 天前图标"改为按数据库引用精确清理，只删除不再被任何剪贴板条目引用的陈旧图标，避免有效图标被误清重建
- **ESC 键逐级返回** (#32) — 按 ESC 键支持智能返回：优先关闭右键菜单/标签选择器等浮层面板，多选状态退回单选（保留一个选中项），设置页/编辑页/后端配置弹窗返回主页，列表页关闭窗口

- **macOS 功耗与内存问题** — 修复 macOS 前台应用检测在 200ms 轮询中反复将 App 图标 `NSImage` 编码为 PNG/base64 占用大量内存和异常功耗问题
- **标签改名跨设备同步** — 标签同步协议新增稳定 `uid`，不再以标签名作为唯一身份；旧标签会在数据库迁移时自动补齐确定性 `uid`，旧同步文件仍按名称兼容。标签改名/改色只更新标签自身，不会刷新所有关联条目的 `updated_at`，避免列表排序被批量顶到顶部
- **多设备同时同步旧快照回写** — 推送前会先将本地快照与刚拉取的云端快照归并，避免某台设备用较旧的本地 `updated_at` 覆盖云端较新的条目，导致两台设备轮流更新同步文件
- **关闭窗口时音效重复播放** — 修复复制内容后关闭来源窗口（如资源管理器、浏览器等）时，系统延迟渲染触发额外音效的问题。通过剪贴板所有者检测和序列号分析区分用户主动复制与系统内部写入
- **F2 备注编辑后键盘导航失效** — 修复按 F2 编辑备注、Enter 确认后键盘 ↑↓ 导航无响应的问题。编辑提交后自动将焦点归还给列表，无需鼠标点击即可继续键盘导航

---

# Version 0.3.3

### Bug 修复

- **图片粘贴失效** (#28) — 修复粘贴图片无响应的问题。将图片剪贴板写入方式从原始 PNG 二进制数据改为 Windows 文件引用（CF_HDROP），使图片能正常粘贴到绝大多数应用中；新增"粘贴为位图"右键菜单选项，将图片解码后写入为原生图片格式（DIB/PNG），兼容不接受文件引用的应用（如部分聊天工具和旧版软件）
- **Windows 自动更新稳定性** — 静默升级时安装器会接收当前 Clippi 进程 PID，并优先等待这个精确进程退出后再覆盖程序文件；若退出超时再按 PID 兜底终止，避免 release/debug、窗口隐藏状态或不同电脑速度差异导致旧进程仍占用 `clippi.exe` 而升级失败；同时保留 UAC 父窗口句柄校验与前台窗口兜底，提升权限弹窗稳定性
- **本地文件夹同步反复更新时间戳** — 修复多设备复制相同内容时，同步可能因为 `updated_at` 反复变化而互相覆盖的问题；同一内容的最新复制时间仍会同步，但不会让普通未收藏副本覆盖用户主动收藏的条目
- **收藏状态同步冲突** — 优化同一 `content_hash` 的合并规则：远端已收藏条目会提升本地同内容副本为收藏；普通未收藏副本不会反向取消收藏；只有明确的取消收藏墓碑才会传播取消收藏意图
- **同步残留数据清洗** — 同步前会清理本地已被新实体推翻的删除/取消收藏/删除标签墓碑；拉取云端数据后会去重并规范化实体、删除墓碑、取消收藏墓碑之间的冲突，避免旧版本残留继续扩散
- **WebDAV 周期同步状态异常** — 修复 WebDAV 后端首次添加可同步、后续周期检查变红的问题；增强目录检查返回码兼容性，并在带 ETag 的条件拉取失败时自动退回普通拉取
- **同步设置双向锁** (#29) — 修复无后端时总开关打不开且无法添加后端的死锁问题：移除空后端时的总开关限制，用户可先开总开关再配置后端
- **图片缓存清理安全性** — 删除条目图片文件时增加目录归属检查，仅删除 Clippi 自身缓存目录内的图片，防止误删缓存目录外的外部文件

### 优化

- **图片卡片显示增强** — 图片卡片在缩略图加载中时显示文件名和图片图标，替代单一尺寸信息，更易识别；文件剪贴板中的单张图片不再额外复制到缓存目录，直接引用原始文件保留元数据
- **图片缩略图可靠性** — 缩略图采用临时文件写入 + 重命名的原子方式，增加 PNG 头部完整性校验，自动清理损坏的缩略图文件并触发生成
- **二维码检测异步化** — 二维码识别改为后台线程使用独立数据库连接执行，不再阻塞主轮询循环，提升界面流畅度
- **复制音效反馈** (#27) — 检测到剪贴板写入时播放短促提示音，支持 6 种音效可选，设置中可开关并点击试听
- **隐藏程序坞图标** (#26) — macOS 通用设置新增"隐藏程序坞图标"选项，开启后仅保留状态栏图标；开关语义与 Windows"隐藏任务栏图标"保持一致
- **同步设备标识修正** — 同步快照的 `device_name` 改为使用本机设备名，不再使用后端名称，避免删除/取消收藏墓碑的设备来源语义混乱
- **同步冲突文件清理** — 本地文件夹同步在内容已一致无需推送时，也会清理已合并的冲突文件
- **呼出窗口时回到首页** (#25) — 新增通用设置选项：开启后每次呼出窗口（快捷键/托盘）自动切回剪贴板历史页面，不再停留在上次的页面（如设置页）
- **i18n 国际化整理** — 清理 72 个未使用的旧 i18n 键（旧版筛选标签、未使用的 toast、纯图标工具栏等），修复 7 处硬编码字符串（扫码/OCR 提示、文件失效标记、跨平台路径标签），新增 5 个规范化 i18n 键覆盖全部硬编码位置

---

# Version 0.3.2

⚠️ Windows 自动更新功能此版本已通过本地构建验证，下版本 Windows 应该就能正常更新了

### 优化

- **同步后端添加界面重构** — WebDAV 和本地文件夹后端添加流程优化，表单布局更清晰，后端名称为空时自动聚焦并提示
- **同步开关联动** — 关闭主同步开关时自动关闭所有独立后端（卡片收起隐藏"立即同步"按钮），重新打开时按记忆恢复，手动关闭的后端不会被误启；后端卡片间距拉开
- **后端删除确认弹窗** — 删除后端时弹出确认弹窗（半透明遮罩 + 缩放动画），防止误操作
- **ConfirmDialog 组件内建动画** — 确认弹窗组件重构为自带动画，调用方只需传 `render_animated(window, cx, generation)`，无需手动维护 opacity/scale 过渡
- **富文本 HTML 内联样式增强** — 从 IDE 或浏览器复制的富文本内容现在能正确渲染更多内联样式：`font-weight: bold`（加粗）、`font-style: italic`（斜体）、`background-color`（背景色）；支持 `<font color="red">` 等经典 HTML 颜色属性；带内联颜色的 `<a>` 链接标签移除链接后保留文字颜色；`<div>`、`<p>`、`<b>`、`<i>` 等更多标签参与样式继承
- **快速粘贴窗口富文本渲染** — 快捷粘贴窗口现在同样支持富文本的内联颜色、加粗、斜体和背景色渲染，与主窗口行为一致
- **快捷键冲突检测与回退链** — 录制时主快捷键与快速窗口快捷键相同会明确提示冲突并保留旧键位；与第三方应用快捷键冲突时旧键位不受影响；首次启动时若配置快捷键被占用，自动从回退链（Windows：V 链 Alt+Shift+V→Ctrl+Alt+V→Win+Shift+V，C 链 Alt+Shift+C→Ctrl+Alt+C→Win+Shift+C；macOS：V 链 Option+Shift+V→Ctrl+Option+V→Cmd+Shift+V，C 链 Option+Shift+C→Ctrl+Option+C→Cmd+Shift+C）选取可用键位并 toast 提示
- **Toast 通知优化** — 错误提示使用红色文字，信息提示使用绿色文字；toast 在设置页面也能正常显示；连续触发时自动重置倒计时

### Bug 修复

- **WebDAV 坚果云同步设置失败** — 修复坚果云 WebDAV 连接验证失败的问题 (#21)
- **系统快捷键冲突时录制失效** — 修复当前快捷键被系统或其他应用占用时，快捷键录制功能无法启动的问题（即使旧键不可用也能打开设置重新录制）(#20)
- **按创建时间排序时快速窗口呼出缓慢** — 修复开启"按创建时间排序"后 Alt+V 快速粘贴窗口长时间无响应的问题，根因是数据库缺少 `created_at` 索引导致全表扫描，已有数据库通过 v5 迁移自动补齐 (#24)
- **多次运行启动多个进程** — 修复单实例保护失效导致可同时运行多个 Clippi 进程的问题，根因是 TCP 端口监听器在检查后立即释放，改为泄漏到进程生命周期持有端口
- **Windows 自动更新弹窗 UAC 缺少父窗口** — 修复 release 构建（无控制台）下 `ShellExecuteW("runas")` 传入空窗口句柄导致 UAC 无提示静默失败的问题，现在传入 Clippi 主窗口 HWND 确保 UAC 弹窗正常显示
- **更新安装流程主线程阻塞** — 修复 `do_update_restart` 使用同步通道阻塞 GPUI 主线程等待安装程序结果的问题，改为异步轮询模式，UAC 弹窗期间 UI 保持响应
- **更新网络请求无超时保护** — 为 GitHub API 查询、安装包下载、SHA256 校验和请求添加连接和读取超时，避免网络异常时后台线程永久阻塞
- **更新提示 Toast 与更新页面重复显示** — 当用户已在设置→版本页面时，不再弹出更新 Toast 通知，避免重复展示

---

# Version 0.3.1

⚠️ Windows 自动更新失败原因已确定，由于更新程序在主线程执行，造成堵塞导致程序异常退出，安装程序没有成功拉起并且旧线程仍然占用安装包，此版本已修复问题并恢复静默安装行为（需弹窗授权），下版本 Windows 应该可以正常使用自动更新功能，此版本若失败请继续尝试手动更新

### 优化

- **周期缓存清理可配置** — 统一清理孤立图片、过期图标缓存和过期同步墓碑；支持设置清理频率（从不/每天/每周，默认每天），提供"立即清理"按钮；应用每次启动时执行一次基础清理，长期运行时执行周期清理 (#19)
- **快速窗口颜色预览** — 颜色类型条目实色预览块替代通用图标，边框根据颜色亮度自适应
- **快速窗口 URL/路径/文件显示对齐** — URL 显示 favicon + 站点名 + 页面标题，路径显示末级目录名 + 完整路径（源丢失变红、跨平台变黄），文件显示对应类型图标 + 多文件显示第一个名和总数，辅内容弱化显示
- **图片右键粘贴路径** — 右键菜单新增"粘贴图片路径"，以纯文本粘贴图片文件路径
- **快速窗口失效条目过滤** — 源文件丢失的图片和单文件条目不再显示

### Bug 修复

- **悬浮工具栏操作目标不一致** — 修复备注按钮点击后可能无响应的问题；悬浮工具栏的备注、编辑、标签等单条目操作会先选中当前悬浮卡片再执行，避免作用到旧选中项
- **列表卡片横向移动** — 修复触摸板横向滑动时列表卡片会左右偏移的问题
- **更新提示"稍后"按钮无响应** — 修复更新提示弹窗中"稍后"按钮点击无效的问题
- **Windows 自动更新安装流程** — 修复在 GPUI 主线程调起安装包可能导致进程异常退出的问题；恢复静默安装流程，安装器启动成功后应用会正常退出并由安装器完成覆盖与重启
- **Windows 自动更新安装目录** — 静默更新会读取已安装版本记录的 `InstallLocation`，避免非默认安装目录被更新到默认目录
- **Windows 自动更新快捷方式选择** — 静默升级会根据现有开始菜单和桌面快捷方式状态恢复组件选择，避免重新创建用户未选择的快捷方式
- **Windows 自动更新残留清理** — 下载前若旧版本残留安装器占用临时目录，会精准终止 `%TEMP%\clippi-update` 内的旧安装器进程并重新下载

---

# Version 0.3.0

### 优化

- **界面 UI 优化** — 部分场景界面优化
- **动效优化** — 部分场景新增轻量过渡动画，优化视觉效果
- **标签面板输入框优化** — 标签面板输入框功能扩展，搜索/创建标签二合一，回车可智能创建新标签或激活已有标签，支持拼音搜索
- **悬浮工具栏标签操作** — 卡片悬浮工具栏新增标签按钮，位于备注和收藏之间，点击可快速为当前条目或批量选择的条目添加/移除标签
- **自动清理旧安装包残留** — 下载更新前自动清理临时目录中过期安装包
- **自动解析URL** — 复制链接时自动获取页面标题，卡片展示站点名和标题，URL 无协议前缀也能识别；标题会随同步数据上传，其他设备缺失时可自动补齐；支持设置开关，默认开启
- **URL 图标跨设备补齐** — URL favicon 不依赖来源应用图标；同步拉取、复制新链接、打开旧链接时都会在当前设备本地补齐 URL 图标缓存
- **路径类型增强** — 路径卡片展示末级目录和完整路径，底部显示盘符标签（含卷标名），支持搜索盘符筛选；UNC 路径和 IP 地址路径可识别跳转
- **源丢失检测** — 路径、图片、单文件类型自动检测源文件是否存在，失效时红色文字和标签标注，跳转按钮自动隐藏；多文件暂不检测
- **跨平台路径** — 非当前平台格式的路径不显示跳转按钮，路径标题和来源平台标签以黄色高亮，支持设置开关过滤隐藏
- **悬浮工具栏优化** — 不再阻止列表滚动；右键菜单滚动时自动关闭

### Bug 修复

- **快速窗口呼出不稳定** — 简化快速窗口关闭逻辑，修复快速窗口触发不稳定的问题
- **标签面板无法滚动** — 标签过多时面板高度固定不变，现已支持滚动
- **部分 URL 和路径无法跳转** — 修复无协议前缀 URL 和 IP 地址路径点击打开位置无反应的问题

---

# Version 0.2.9

### 优化

- **渲染优化**
  - 卡片内容文本按需加载，不再每帧复制完整文本
  - 快速窗口标签列表仅加载已固定的标签，减少无用数据拷贝
  - 应用图标和文件图标缓存目录在启动时预创建，渲染过程零磁盘写入
  - 图标缓存未命中时不再阻塞界面，改为后台线程处理
- **同步可靠性增强** — 同步过程中数据库操作异常（如记录删除标记、更新收藏状态等）添加详细日志记录，便于排查同步问题
- **数据库检查点异常日志** — 窗口关闭和剪贴板变更时触发的数据库持久化操作，异常情况写入日志便于诊断
- **macOS 快速窗口视觉优化** — 快速窗口恢复系统阴影并保持圆角裁剪，提升浮动面板层次感
- **代码结构优化**
  - 剪贴板内容检测逻辑按类型拆分为独立子函数，维护性更好
  - 设置页开关控件提取为公共组件，减少重复代码
  - 热键录制卡片提取为公共组件，主热键和快速热键 UI 保持一致
  - 同步后端添加/编辑操作提取公共辅助函数

### Bug 修复

- **WAL 检查点静默失败** — 修复数据库变更持久化失败时无任何提示的问题
- **macOS 快速窗口光标定位** — 增强 Accessibility 光标范围解析，支持浏览器 text marker 路径，过滤窗口级错误矩形；无法获取可靠光标时回退到鼠标位置，并优化光标底部对齐

---

# Version 0.2.8

### 新功能

- **自动更新** — 支持从 GitHub Releases 自动检查、下载、校验并安装新版本
  - 启动时和每 24 小时自动静默检查更新，发现新版本时托盘图标显示圆点提示
  - 设置新增「版本」页面，显示当前版本、更新状态、Release Notes，一键下载更新
  - 下载完成后 SHA256 校验，自动重启安装
  - 下载和重启操作通过 Toast 通知展示，支持亮色/暗色主题适配

### Bug 修复

- **快速窗口定位问题** — 快速窗口光标定位沿用了记录上一个活动窗口的逻辑，但由于快速窗口不会抢走焦点，导致获取目标不准确，改用实时前台窗口定位，提升定位稳定性
- **多显示器 DPI 适配** — 修复 Windows 在不同缩放比例的显示器间呼出主窗口或快速窗口时，GPUI 缩放比例未及时刷新、窗口定位偏差、首次启动未恢复用户记录的窗口尺寸，以及快速窗口边缘出现空隙的问题

### 优化

- **优化快速窗口 UI** — 优化列表圆角，优化四边间距
- **托盘入口窗口定位优化** — 托盘菜单（显示窗口、设置、检查更新）打开窗口时始终使用记住位置模式，避免窗口贴屏幕底部边缘打开的问题
- **检查更新导航优化** — 托盘"检查更新"改为自动打开设置→版本页面，方便直接查看更新状态和下载更新

---

# Version 0.2.7

### Bug 修复

- **数据目录重置按钮无响应** — 修复非便携模式下点击"重置"按钮无反应的问题
- **便携模式反复跳变** — 修复便携模式检测不稳定，导致数据目录在 exe 目录和系统目录之间来回切换的问题
- **系统主题无法实时跟随** — 修复设置主题为"跟随系统"时，切换 OS 深色/浅色模式需重启才能生效；同时修复快速窗口在"跟随系统"模式下始终显示为暗色主题的问题 (#18)
- **快速窗口冷启动无法跟随输入光标** — 修复刚启动 Clippi 时，快速窗口无法定位到当前应用输入光标位置的问题

### 优化

- **数据目录切换智能合并** — 切换数据目录时，如果目标位置已有数据，自动合并数据库和配置文件，不再直接覆盖导致数据丢失
- **快速窗口备注优先显示** — 有备注的条目优先显示备注文字，邮箱和电话类型自动脱敏处理；开启"悬停显示原文"设置后，选中有备注的条目显示原文，与主窗口行为对齐
- **快速窗口标签栏弹性布局** — 固定标签少时按文字自然宽度显示，满一行时自动平分宽度；标签文字过长时省略号截断并显示悬停提示；无固定标签时自动隐藏标签栏并收起窗口高度

---

# Version 0.2.6

### 新功能

- **快速粘贴窗口** — 新增可独立开启（设置 - 通用 - 快速粘贴窗口）和配置快捷键（设置 - 快捷键 - 快速窗口快捷键）不抢焦点的快速窗口，在系统应用中优先跟随光标位置，仅支持类型筛选、固定标签、快速粘贴等基础操作；可使用 `↑/↓` 选择、`←/→` 翻页、`1-5` 数字键直接粘贴当前页条目、支持鼠标点击和滚轮导航、`Enter` 粘贴、`Esc` 关闭，呼出时不会抢走应用焦点，适合在不打断输入的场景下进行快速粘贴，编辑等高级操作扔需打开主窗口进行
- **编辑面板智能类型切换** — 在编辑面板中将富文本类型（HTML/Markdown）切换为纯文本类型（链接、路径等）时，自动剥离 HTML 标签并规范化空白行，仅显示纯文本；切回富文本类型时，自动恢复原始标签；在纯文本模式下编辑的内容会同步回 HTML 文本节点中
- **键盘快捷键支持** — 为剪贴板列表和搜索栏添加完整键盘快捷操作：`Shift+Enter` 粘贴为纯文本、`Ctrl+F` 聚焦搜索框、`Ctrl+D` 切换收藏、`Ctrl+E` 编辑、`F2` 添加/编辑备注、`Delete` 删除、`Escape` 清除选择；↑↓ 导航条目；搜索框聚焦时 ↑↓ 可在不失焦的情况下导航列表，按操作快捷键时自动切回列表并执行对应操作

### 优化

- **快捷键录制响应** — 录制期间使用独立快速采样，短按一次即可识别，录制完成后立即刷新设置界面

### Bug 修复

- **托盘菜单进入设置时显示 Clippi 自身** — 修复右击托盘图标弹出菜单时，焦点监听器误将 Clippi 进程内的弹出菜单窗口识别为外部应用，导致快捷键设置页的"当前应用"栏显示 Clippi 自身，可能造成误加入黑名单或误配置粘贴快捷键的问题
- **静默启动窗口闪烁** — 修复静默启动（开机自启）时窗口轮廓和任务栏图标短暂闪现的问题。通过 GPUI `WindowOptions { show: false, focus: false }` 让窗口在创建时就保持隐藏，首帧渲染后释放 in-memory 条目列表，同时保持低内存占用
- **编辑保存后类型未即时刷新** — 修复编辑面板保存 link/path/color 类型条目后，卡片上的类型标签和图标未即时更新（需重开窗口才能正确显示）的问题。根因是 `storage_for_editor_type` 将 link/path/color 存为旧版 content_type 格式，未写入 v4 meta_type，导致 `display_kind()` 无法识别，回退为纯文本显示
- **macOS 路径选择未响应** — 修复更改数据库路径或添加本地文件夹同步后端时，原生文件选择窗口可能让应用进入未响应状态的问题。文件对话框改为异步执行，避免 GPUI 主线程等待对话框结果，同时保留 Windows 端规避 COM 对话框冲突的修复 (#16)

---

# Version 0.2.5

### 新功能

- **可自定义类型筛选栏** — 右键筛选栏可配置类型按钮的显示/隐藏和排列顺序，窗口较窄时自动切换为纯图标模式，配置持久化保存
- **扩展筛选类型** — 类型筛选按钮新增图片、路径、联系信息（邮箱和手机号），各类型独立筛选不再捆绑
- **独立粘贴快捷键配置** — 快捷键设置页新增按进程配置粘贴快捷键功能，可为不同应用录制自定义粘贴组合键（如 Shift+Insert），支持列表管理、重新录制和确认对话框键盘操作（Enter 确认 / Esc 取消）
- **拼音搜索** — 支持用全拼和首字母缩写搜索中文内容

### 优化

- **统一内容类型分类** — 优化内部类型存储结构，链接、路径、颜色归为纯文本的子类型，与邮箱、手机号保持一致
- **编辑面板可拖拽分隔手柄** — 富文本编辑模式下，内容编辑区和预览区之间新增拖拽手柄，可手动调整两部分比例，初始各占一半，自适应窗口大小
- **编辑面板工具按钮提示文本** — 为编辑面板工具栏按钮（返回、URL解码、Base64解码、JSON格式化、文本修剪）添加鼠标悬停提示
- **标签面板按钮提示文本** — 为标签筛选面板的功能按钮（切换筛选模式、清空筛选）和标签选择器清空按钮添加悬停提示
- **更新图标** — 替换右键菜单粘贴图标和 OCR 图标，更新标签面板清空按钮和文件筛选按钮图标
- **统一下调 Tooltip 字体** — 全局 tooltip 提示文本字体大小从默认统一下调
- **同步状态指示灯优化** — 指示灯改用图标字体渲染，避免像素偏移
- **悬浮工具栏图标优化** — 链接、路径、文件类型的"打开位置"按钮改用线条风格独立图标（浏览器 \u{e643}、文件夹 \u{e609}），视觉风格与其他工具栏按钮统一
- **右键菜单补充打开操作** — 链接、路径、文件类型右键菜单新增"打开链接"/"打开文件夹"选项，与悬浮工具栏功能对齐
- **智能粘贴快捷键** — 粘贴时自动检测终端/控制台窗口（如 Windows Terminal、conhost），使用 Shift+Insert 替代 Ctrl+V，解决终端中粘贴失效问题

# Version 0.2.4

### 新功能

- **屏蔽系统窗口行为** — 通用设置中新增开关，可禁用双击标题栏全屏和窗口贴边缩放等系统默认窗口行为，默认关闭 (#15)
- **自动聚焦搜索框** — 剪贴板设置中新增开关，打开窗口时自动聚焦到搜索框，默认关闭

### 优化

- **卡片自定义标签溢出显示** — 空间不足时隐藏超出的自定义标签并显示 `+N` 数量，窗口宽度变化后自动更新，避免标签被裁切
- **文件类型预览优化** — 多文件预览从 3 个扩展到 4 个；每行使用各自真实的系统文件图标（优先缓存，同类型文件共用缓存），不再使用通用图标；左侧类型标签固定显示"文件"，文件数量已在标签中展示

### 稳定性

- 增强剪贴板监听和同步服务的异常恢复能力，避免个别错误导致监听中断
- 优化数据库查询性能，提升大量条目时的筛选和列表响应速度
- 改进数据序列化和同步时间戳解析的容错性，减少潜在的同步异常

# Version 0.2.3

### Bug 修复

- **语言切换后输入框文本未更新** — 切换语言后，搜索框、标签面板、编辑面板和后端配置中的提示文本会立即更新

### 优化

- **备注卡片高度** — 添加备注后减少卡片空白，取消备注后恢复自适应高度
- **macOS Intel 支持** — 提供 Apple Silicon 和 Intel 对应的独立安装包 (#13)
- **窗口首次显示** — 修复窗口首次打开时短暂闪现错误尺寸的问题
- **隐藏任务栏图标** — Windows 可选择隐藏 Clippi 的任务栏图标 (#14)

# Version 0.2.2

### Bug 修复

- **回车键操作冲突** — 打开编辑或浮层时，回车不再意外触发粘贴
- **富文本格式丢失** — 使用"粘贴为纯文本"后不再破坏原条目的富文本内容
- **双击粘贴格式异常** — 双击富文本条目时会保留完整格式
- **同步状态显示异常** — 未发生有效同步时不再错误刷新最后同步时间
- **多端同步统计不一致** — 不同设备会显示一致的同步条目和标签数量
- **备注无法同步** — 修改备注后可正常同步到其他设备
- **安装时未提示退出程序** — Windows 安装包可检测正在运行的 Clippi 并提示退出。由 0.2.1 及更早版本升级时，建议先手动退出旧版本
- **文件选择窗口闪退** — 修复浏览同步文件夹或更改数据库位置时可能闪退的问题

### 优化

- **内容类型显示一致性** — 卡片预览、类型标签和编辑面板对内容类型的显示保持一致
- **纯文本粘贴选项** — 纯文本条目不再显示无意义的"粘贴为纯文本"
- **定位最新条目** — 原"滚动到顶部"选项调整为"定位最新条目"，打开窗口时可自动选中并定位最近更新的内容
- **保留列表位置** — 编辑内容或标签后，列表不再突然跳动
- **界面细节** — 统一多处视觉和交互效果

# Version 0.2.1

### Bug 修复

- **构建版窗口图标不显示** — 修复标题栏、任务栏和 Alt+Tab 中的图标显示
- **固定窗口不置顶** — 固定窗口后可保持在其他窗口上方
- **托盘设置无法打开窗口** — 从托盘进入设置时会正确显示窗口
- **托盘退出无效** — 退出和重启操作可正常结束程序
- **macOS 全局快捷键失效** — 恢复 macOS 全局快捷键
- **macOS 快速粘贴闪退** — 修复剪贴板读写冲突导致的闪退
- **macOS 快速粘贴权限异常** — 改善构建版的辅助功能权限识别
- **macOS 应用签名失效** — 避免应用数据写入安装包并影响签名
- **重复复制时间不更新** — 再次复制相同内容时会刷新条目时间
- **标签操作后列表不跟随** — 添加或移除标签后会自动定位到对应条目

### 优化

- **macOS 发布包校验** — 改善 macOS 安装包的版本和签名校验
- **链接图标显示** — 恢复链接条目的网站图标
- **卡片尺寸标签** — 恢复文本字符数、图片尺寸和文件大小显示
- **窗口默认选中** — 打开窗口时自动选中并定位最近复制的条目

# Version 0.2.0

### 重大变更

- **界面架构全面升级** — 改善界面性能、富文本显示、列表滚动、键盘导航和后续扩展能力
- **富文本预览和编辑** — 支持 HTML 与 Markdown 内容的预览和编辑
- **键盘列表导航** — 可使用方向键浏览并定位剪贴板条目
- **即时语言切换** — 切换语言后无需重启应用

### 新功能

- **粘贴为纯文本** — 富文本条目可选择仅粘贴文本内容
- **Enter 键粘贴** — 可使用回车快速粘贴选中条目
- **便携模式** — 支持将数据保存在安装目录，并可在设置中切换数据位置

### Bug 修复

- 修复窗口在焦点切换期间闪现或关闭的问题
- 修复部分高版本 macOS 无法快速粘贴的问题

### 优化

- 二维码条目显示完整图片，与普通图片条目更容易区分

# Version 0.1.3

### 新功能

- **文本修整工具** — 可清理多余空格、空行和 Unicode 空白字符
- **Base64 解码工具** — 支持标准、URL 安全和 Data URL 格式
- **二维码识别** — 支持自动识别和手动识别图片中的二维码

### Bug 修复

- 修复定时同步无法拉取远端更新的问题
- 修复取消收藏状态在多端同步时异常的问题

### 优化

- **主动同步** — 开启同步或启动软件时会主动检查远端变化
- **敏感信息脱敏** — 邮箱和手机号预览默认隐藏部分内容
- **缓存自动清理** — 自动清理无效图片、缩略图和长期未使用的图标缓存

# Version 0.1.2

### 新功能

- **WebDAV 同步** — 支持使用 NAS、Nextcloud 等 WebDAV 服务同步数据
- **同步后端独立管理** — 每个同步后端可单独启用并设置同步间隔
- **JSON 格式化工具** — 支持格式化和压缩 JSON 内容

### Bug 修复

- 修复 macOS 双击粘贴失效的问题
- 修复触控板滚动列表时跳动的问题
- 修复多端同步统计不准确的问题
- 修复 Windows 部分中文显示为方框的问题
- 修复 macOS 右键菜单位置偏移的问题
- 修复编辑面板缺少邮箱和电话类型的问题

### 优化

- 优化卡片图标、标签样式和信息排列
- 提升 Windows 中文图片 OCR 的识别效果
- 为编辑面板工具按钮增加悬浮提示

# Version 0.1.1

### 新功能

- **侧边标签栏** — 支持标签筛选、展开收起和固定常用标签
- **版本更新检查** — 支持启动检查和托盘手动检查更新
- **邮箱和电话识别** — 为邮箱与电话号码显示对应类型
- **图片 OCR** — 支持识别、搜索和粘贴图片中的文字
- **URL 解码工具** — 可还原百分号编码的 URL 内容

### Bug 修复

- 修复快捷键录制后重启丢失的问题
- 修复编辑备注后关闭窗口的状态残留
- 修复过长文件路径溢出卡片的问题
- 修复内部复制和 OCR 粘贴产生重复条目的问题
- 修复多项标签、收藏、删除和重建状态的跨设备同步异常
- 修复部分本地变更无法推送的问题
- 修复同步时间和删除状态冲突的问题

# Version 0.1.0

首个公开版本，支持基础剪贴板管理、标签、文件夹同步，以及文本、图片、文件、链接、颜色和富文本等内容类型。
